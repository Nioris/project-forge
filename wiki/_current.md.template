# Current Session State

> Этот файл — текущий пульс работы. Claude обновляет его после каждого
> значимого шага. Держать коротким: 20–30 строк. Если разросся — значит
> часть надо переносить в `_map.md` или `features/<n>.md`.
>
> Инжектится в контекст на каждом старте сессии и после /compact.

## Session goal

_Одна строка: что решаем в этой сессии._

Пример: «Довести до рабочего состояния VK Bridge авторизацию и сохранить токен в dexie».

## Active task

Текущая задача из `wiki/_map.md → Next`. Список шагов с чекбоксами.
Последний пункт без галочки = то, над чем работаем прямо сейчас.

- [x] Шаг 1
- [x] Шаг 2
- [ ] Шаг 3 ← здесь сейчас
- [ ] Шаг 4

Файлы в работе: `src/...`, `src/...`

## Blockers

_Пусто или 1–3 строки. Что не даёт двигаться дальше._

## Last 3 decisions

_Последние архитектурные решения этой или предыдущих сессий. Полный список — в `wiki/decisions/`._

- YYYY-MM-DD: суть → [[decisions/NNN-имя]]
- YYYY-MM-DD: суть → [[decisions/NNN-имя]]
- YYYY-MM-DD: суть → [[decisions/NNN-имя]]

## Not yet documented

_Список того, что сделано в коде, но ещё не попало в wiki. Хук `wiki-audit`
автоматически блокирует `stop` пока этот список не пуст._

- [ ] `wiki/features/<n>.md` — для модуля `<n>`
- [ ] `wiki/decisions/<NNN>-<n>.md` — для выбора `<X>`
- [ ] `wiki/changelog.md` — запись по сегодняшнему commit

## Notes for next session

_Что важно не забыть при возвращении. 1–5 строк._
