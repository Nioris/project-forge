# Project Forge — Project Map

> Living map of Forge as a meta-project (a bootstrapper for other projects).
> Updated by Claude after each session. Open as Obsidian vault for full wiki view.

## Vision

**Project Forge** — opinionated bootstrapper для multi-platform HTML5/PWA проектов с автоматическим release pipeline для 9 платформ (Yandex Games, VK Mini Apps, Telegram Mini App, OK.ru, MAX, RuStore, Web self-hosted, Steam, VK Play).

### Кому нужно

- Solo-разработчики и микро-команды которые делают игры/приложения и хотят релизить на несколько платформ без ручного 18-touchpoint refactor'а на каждую
- Yandex Games / Telegram Mini App focus — российская специфика учтена (13 языков, юр. требования, RuStore)
- Тем кто работает с Claude Code как primary IDE и хочет structured persistent memory через wiki/

### Почему Forge a thing

Без Forge каждая новая платформа = ~18 файлов изменений + drift. Forge превращает это в:
1. Skill `/release-{platform}` запускает full pipeline
2. Validators запускают gate перед релизом
3. Hook'и enforce architectural rules (workspace discipline, plan check)
4. Verifier scripts проверяют structural integrity (platform completeness, inline strings, etc)

## Stack

См. [[architecture/stack]]

Кратко:
- **Core**: Node.js 20+, ES modules (.mjs), bash + PowerShell для setup
- **Tooling**: только встроенные модули Node (`fs`, `path`, `child_process`) — no runtime deps
- **Skills**: 142 canonical markdown workflows в `.claude/skills/{name}/SKILL.md` + 3 generated Codex routers
- **Hooks**: 8 .mjs файлов в `.claude/hooks/`
- **Agents**: 21 canonical markdown roles в `.claude/agents/` + generated Codex TOML mirrors
- **Knowledge base**: 61 SKILL.md в `./skills/` (referenced by command skills)
- **Platforms**: 9 директорий в `platforms/` с validators + scripts + templates

## Features

- **9 platforms** — yandex, vk, telegram, ok, max, rustore, web, steam, vkplay
- **34 validators** — pre-submit gate per platform
- **142 canonical skills + 3 Codex routers** — phase workflows, orchestrators and specialist sub-skills
- **8 hooks** — workspace-discipline, plan-check, wiki-audit, session-start, status-line, stop-flush, post-tool-capture, block-dangerous
- **21 agents** — platform builders + design/art/QA/security/research/AI Studio specialist roles
- **Dashboard** — `dashboard.html` для project tracking + quick prompt copy
- **i18n foundation** — runtime ru+en architecture from day 1 (v4.7.6)
- **Workspace discipline** — 3-folder enforcement (GameIntegration/WorkProgress/Release) (v4.7.7)
- **Context-aware advisor** — reads wiki/ before formulating prompts (v4.7.5)
- **Platform completeness audit** — 18 checks × 9 platforms = automated drift detection (v4.7.0)

## Current Status

### Version: v4.68.16 (latest released)

- 2026-08-18: **v4.68.16** — Quality-Sol Codex routing on Standard with bounded per-phase context, automatic local Git checkpoints, secret/public-remote guards and opt-in private GitHub creation/push for every future game/app.
- 2026-08-18: **v4.68.15** — Windows Yandex release pipeline: external project roots, native cross-platform ZIP handling, correct production/debug/marketing contents, ZIP-aware CDN/runtime checks and staging-to-release listing resolution.
- 2026-08-18: **v4.68.14** — GigaChat mature-phase orchestration: immutable ordered phases, bounded context/skill loading, canonical tool-call recovery, durable verifier evidence, reliable browser QA helpers and real promo capture.
- 2026-08-18: **v4.68.13** — GigaChat durable-answer integrity and non-bypassable phase completion: pending STOP answers restore phase context, runtime ledgers are protected, decision markers auto-block, and every completion path requires GREEN plus evidence.
- 2026-08-18: **v4.68.12** — durable GigaChat Phase 1 resume: approved research/KPI decisions survive phase switches, final completion avoids a repeated model turn, host metadata stays honest, stale nested STOP text is removed, and exhausted empty responses are diagnosed.
- 2026-08-18: **v4.68.11** — local fleet behavioral diagnostics: AI/adapter/hook/runtime incident reporting, secret-redacted JSONL with stable fingerprints and verified resolution, status counters, and a sibling-wide read-only auditor.
- 2026-08-18: **v4.68.10** — Codex economy routing: Standard tier, per-phase Terra/Sol/Luna policy, policy-aware fresh-task launcher, maximum two Terra/medium subagents, Dashboard launch commands and honest actual-vs-recommended model state.
- 2026-08-18: **v4.68.9** — GigaChat 6.3.3 guided STOP answers: every user gate prints `Как ответить`, the exact `утверждаю` approval phrase and a decision-specific correction/deepening format; Phase 1 brief includes a complete Q1–Q5 template.
- 2026-08-18: **v4.68.8** — GigaChat 6.3.2 natural brief acceptance: explicit phrases such as `принимаю рекомендации` approve the complete Q1–Q5 set, while qualified corrections remain unresolved; exact real-entrypoint regression added.
- 2026-08-18: **v4.68.7** — GigaChat 6.3.1 resume orchestrator: repaired partial Phase 1 decisions and stale runtime evidence, fixed malformed ask_user crash, added live search/fetch + doctor/self-tests, automatic system CA/no-key fallback launcher setup, and semantic API-profile release gates.
- 2026-08-16: **v4.68.6** — rebased the verified local-first release onto the concurrent GitHub/GitVerse mirror workflow and included that automation in the canonical MANIFEST.
- 2026-08-16: **v4.68.5** — Windows PowerShell reads UTF-8 MANIFEST explicitly, preserving Cyrillic managed paths and restoring the full 424-file sibling payload.
- 2026-08-16: **v4.68.4** — reliable Windows one-click update: tar-first extraction, fail-fast fallback, ASCII/CRLF-safe batch entrypoints, `.gitattributes`, and a real `cmd.exe` self-test gate.
- 2026-08-16: **v4.68.3** — release-consistency patch: completed the bilingual Dashboard publication, aligned all generated/versioned surfaces, hardened partial-bump repair, and restored public README platform completeness.
- 2026-08-14: **v4.68.1** — terminal API profiles: Claude subscription/API, Codex ChatGPT/API with isolated auth, Forge-owned GigaChat terminal agent, centralized `forge-data/secrets/`, and dedicated Dashboard buttons; GigaCode CLI bridge remains optional/dormant.
- 2026-08-14: **v4.68.0** — Universal Agent Runtime: host-neutral `FORGE.md`, adapter registry/launcher, managed GitVerse rules and experimental GigaCode bridge; AI Studio schema 2 adds direct GigaChat `text2image` + `text2model3d` providers inside the existing 9 phases.
- 2026-08-14: **v4.67.1** — `/status` rebuilt around the canonical 9 phases: machine `wiki/phases/phase-N.json` markers + conservative artifact fallback, CURRENT/STOP-point, AI Studio and Project Health; future-phase absence is `not reached`, not FAIL. All phase skills write start/block/complete markers. New project CLAUDE no longer stores mutable `Just created` state; state lives in wiki/_current + wiki/phases.
- 2026-08-14: **v4.67.0** — AI Studio встроен в существующие 9 фаз (отдельной Ф10 нет): F1 config/baseline, F2 briefs+prompt packs, F3 phase-aware studio agents, F4 Prompt Compiler → Image Studio → Art Director → Visual QA, F5 AI config/secrets/perf, F6 store creatives, F7 multi-agent/visual QA, F8 provenance/release hygiene, F9 measured creative A/B. 4 new skills + 4 agents; Codex-native ImageGen primary, direct OpenAI GPT Image 2 batch optional via .openai_key, OpenRouter removed as primary image dependency; Codex skill descriptions compacted only in generated discovery metadata.
- 2026-08-14: **v4.66.10** — Windows Codex lifecycle hooks now use direct Node `commandWindows` launchers instead of nested PowerShell; compatibility audit rejects nested PowerShell and missing hook targets.

- 2026-08-14: **v4.66.9** — Codex portable config drops optional `[agents]` scalars for older-CLI parser compatibility; explicit Codex Full launch also bypasses hook trust while preserving the one-time project trust gate.

### Done — major milestones

- 2026-08-14: **v4.66.8** — Fleet sync now snapshots managed sources before propagating to siblings, so an external AV/indexer cannot delete a generated adapter file mid-run and leave a half-synced fleet. `extract_says.js` was rewritten as a static literal dialogue scanner with no `vm`/eval execution.

- 2026-08-14: **v4.66.7** — Dashboard cards split launch into Claude Full + Codex Full (`-a never -s danger-full-access`); added optional `cx`; upgrade now backs up/removes six obsolete pre-construct phase directories so filesystem skill counts cannot drift; Windows updater ZIP discovery hardened after field failure.

- 2026-08-12: **v4.66.6** — Unified Claude/Codex command layer: Claude `/skill`, Codex `$skill`; dashboard version/count/mappings generated and drift-gated; `new-project --type game|app`; one managed `sync.mjs` with `.forge-managed.json`; hardened external semver-aware `update-forge.bat` with fleet verification.

- 2026-08-02: **v4.66.5** — API keys: 🔑 core section with fixed filenames (.openrouter_key, .elevenlabs_key, .pixellab_key at game root or one level up), READ the file rather than hunting through configs/other projects, never print contents, never commit, MCP with -s user. CRITICAL side-finding: .gitignore mask was `*.key` which does NOT match dot-prefixed names — every project git-initialised yesterday would have committed its keys on first `git add -A`. Fixed with `.*_key` in new-game and git-init-games, verified all four excluded.

- 2026-08-02: **v4.66.4** — PixelLab MCP connection command fixed to use `-s user` scope (field: server was bound to another project, no tools in the game — claude mcp add defaults to PROJECT scope, which with 24 folders guarantees 'tools aren't here'). Skill now states the reason and the diagnostic path: claude mcp list (scope), /mcp (session sees it), claude mcp remove in the old project, restart session for tools to appear.

- 2026-08-02: **v4.66.3** — git noise closed at the LAYER, not the symptom (4th report; I'd fixed hook bodies, a verifier, then only the 2 Stop wrappers — settings.json holds EIGHT wrappers and SessionStart/4x PreToolUse/PostToolUse still had unguarded git rev-parse). All 8 regenerated from one template (try/catch, stderr suppressed, cwd fallback, missing-hook exit 0); verified by RUNNING every hook of every event in a project with .git removed — 8/8 silent. Full layer audit: hook bodies 4 git calls guarded (stdio + isGitRepo), wrappers 8/8, plugin-hooks clean, only unix dep left is asset-scan archive listing (has Windows branch), external calls = git (guarded) + npx eslint/prettier (behind existsSync). PROCEDURE: when an environment assumption breaks, enumerate every place that layer can hold it — the reporting file is evidence, not scope.

- 2026-08-02: **v4.66.2** — third git dependency found, this one INSIDE settings.json: Stop hooks are launched by a wrapper that resolves the repo root via git rev-parse --show-toplevel and passes it as cwd, so without a repo it threw before the hook ran. Wrapper now try/catches with stderr suppressed, falls back to cwd, and skips missing hook files with exit 0. Verified in a real project with .git removed. RULE: when an environment assumption breaks, grep the WHOLE tree for that assumption — I fixed the reporting file twice and missed the wrapper both times (hook bodies v4.65.2, verifier v4.66.0, wrapper v4.66.2).

- 2026-08-02: **v4.66.1** — audit #7 after settings.json/git changes: nothing broken. New project end-to-end OK (137 skills, settings.json, 4 commands, git clean tree, brief); ALL 8 hooks silent (root-fixed, not muted); .gitignore correct both ways (code tracked, video/boards/.env excluded); sync into git-backed project fine; MCP 20/20; chain 1→9; check-workspace-discipline no longer failing. 4 verifier exits = game-targeted checks on the engine folder. 2 grep 'orphans' cleared by reading files (target-frame IS in Ф4, new-game.mjs reached from bat+docs) — same false-negative class as audit #5.

- 2026-08-02: **v4.66.0** — games get a LOCAL git (user's question after the third 'not a git repository' patch: why don't we create one?). History: new-game inherited forge.ps1's worktree shape where git was borrowed from the engine; dropping worktrees in v4.44.0 dropped git and replaced it with nothing — while still writing .gitignore into repo-less projects. Real cost: pre-claim-fixed (catches 'I fixed it' with no edits) works BY git diff, so it was dormant in every game. new-game now git-inits + first commit after the payload lands (209 files, clean tree) with a .gitignore excluding video/contact-sheets/boards/node_modules/.env/*.key; NEW git-init-games.mjs does existing projects (--dry, skips existing, NO remote ever); check-workspace-discipline exits 0 silently when there's no repo.

- 2026-08-02: **v4.65.2** — hooks silenced outside git repos (side effect of v4.65.1: settings.json finally reached games, hooks started RUNNING there, games aren't repos → git status/log wrote to stderr on every Bash call → Claude Code showed 15x 'PreToolUse:Bash hook error'). pre-claim-fixed got isGitRepo() early exit; it and wiki-audit run git with stdio ignore-pipe-ignore. Verified: all 7 hooks silent in a non-git dir. Self-note: two regex patches broke wiki-audit's multi-line import and execSync — restored from zip, applied one targeted edit; this is the non-unique-anchor class from v4.32.3, done to myself twice in one task.

- 2026-08-02: **v4.65.1** — CRITICAL: new projects had .claude WITHOUT settings.json, so hooks were never registered — every project created since v4.44.0 had hook files but no wiring (approval-gate, block-dangerous, workspace-discipline, plan-check, session-start, post-tool-capture, stop-flush all silently inert). Sync payload now includes .claude/settings.json and .claude/commands; verified on a fresh project (4 hook events registered, approval-gate present). Surfaced by sync skipping projects with '.claude exists but settings.json unreachable'. Also noted: update-forge.bat still calls deprecated sync.ps1 at step 3/4.

- 2026-08-02: **v4.65.0** — NEW /pixel-art-pipeline. Research finding: PixelLab integration EXISTS officially as an MCP server (claude mcp add pixellab), so the skill decides WHEN to use it, not how to talk to it. PixelLab does what diffusion can't: 4/8-direction characters, ANIMATIONS attached to a character (walk/run/idle), Wang tilesets, isometric tiles, style-reference consistency, true transparency, exact sizes. Skill opens with 🔴 user decision (PixelLab / own OpenRouter / mixed) recorded in brief; money rules: approve style on ONE character, store character_id in wiki (animations attach to it), fix frame size once, treat as expensive batch work. Shared rules: power-of-two frames, 16-32 palette, legibility at real cell size and in greyscale (silhouette not colour), true alpha, asset-catalog at the end. Alternative noted: pixel-mcp for exact per-pixel control.

- 2026-08-02: **v4.64.0** — three adoptions from abagames/agentic-gamedev-skills (MIT). NEW /gameplay-balance: exploratory_ratio = smart policy best / dumb policy max, verdicts mono_dominant & tied = DESIGN defects (back to Ф2, not fixable in code), marginal = structure before numbers; instrument check before judging design (flat search = measurement failure); non-deterministic band protocol for browsers (n>=3 bands, n>=5 to claim a shift, record tails, retraction is normal); ABANDONMENT criterion → Failure Report after 3 attempts or core-level cause. Ф2 must admit nameable dumb+smart policies, Ф7 measures. CORE: batch-work gating generalised (name irreversible resources incl. user's first impression, split point, MECHANICAL stop not a rule, reversible pass over EVERY item, per-item report). visual-upgrade: feedback vocabularies by event meaning (danger/reward/state/near-miss, explicit none, presentation never corrupts mechanics).

- 2026-08-02: **v4.63.0** — 🎯 TARGET FRAME (user's idea = the industry's target frame): 3 variants of the whole MAIN SCREEN in real resolution, prompted as 'screenshot of a browser game', deliberately different compositions → board → user picks → assets/target/target-frame.png; all production aims at it. Closes the day's core gap: the executor never had an image of what 'good' means FOR THIS GAME (references = others' games, bible = isolated elements). Honest constraint stated up front: generation shows unreachable detail, so approval marks what's taken (composition/palette/density/placement) vs not, recorded in wiki/design/target-frame.md. ui-review now measures DISTANCE TO TARGET («до цели: 3 divergences», fixable vs unreachable) instead of taste; frame changes only by new 🔴.

- 2026-08-02: **v4.62.0** — 🪜 discovery ladder in phase 2 (FTUE gap: tutorial was wired across Ф1/Ф2/Ф3/Ф7 but everything covered the FIRST SESSION; days 2-4, where players actually leave, were designed nowhere). Mandatory table: session / what OPENS / why return tomorrow / what holds now. Rules: ONE discovery per session; an opening is an EVENT not an active tab; every session ends unfinished with a visible goal; the reason to return must change (curiosity → invested progress → people); check each rung against the content budget. Verified in Ф7 (sessions 1-3 with closures between). product-metrics links it as the bridge inside storey one.

- 2026-08-02: **v4.61.1** — audit #6 (full health check, every ✗ verified against the file first). GREEN: chain 1→9 with Ф9 looping back, all 10 session additions reachable from the right phases, ZERO orphan skills/agents, hooks parse with 4 events registered, MCP 20/20, debugcheck copies identical at 119 checks, AGENTS.md fresh, user data out of the distribution (seed only), no unix-only deps left. Five verifier 'failures' were game-targeted checks run against the engine folder — expected. One real finding fixed: cleanup-orphan-wrappers.mjs was an orphan AND a duplicate of sync.mjs pruning (since v4.49.2) — removed. Standing: 135 skills, 17 agents, 564 files.

- 2026-08-02: **v4.61.0** — project BRIEF in phase 1 after prototype analysis (user: grilling belongs after the analysis, and audience age surfaced only in Ф6 при заполнении рейтинга — too late). Five things code can't tell: audience (age/who → pacing, difficulty, text, rating), ambition (2 weeks vs 3 months → scope), the PROMISE (what the player should feel, concrete not genre-speak), the difference from catalogue peers, history of failed attempts. One round in /grilling format with recommended answers → wiki/design/brief.md; phases 2/4/6 read it instead of re-asking. Full 45-min grilling stays opt-in (mandatory long interview would just be skipped). Template shipped and copied by new-game.

- 2026-08-02: **v4.60.1** — /find-skills wired into phase-4 as Step 0.0 (ecosystem check for the game's specific task before designing anything: pixel art, game ui, icon design, sprite animation, ui kit, accessibility) with the two always-worth-it installs named (frontend-design, web-interface-guidelines); art-direction checks before the reference board. Bounded: selection hygiene, pointwise installs, ONCE per project, one-line fallback.

- 2026-08-02: **v4.60.0** — /grilling (Matt Pocock, MIT, body verbatim: design-tree rounds, frontier = decisions whose prerequisites are settled, whole frontier per round, ❓Q1/➡️ format with a recommended answer each, facts are the agent's job via sub-agents, decisions are the user's, nothing acted on until shared understanding confirmed) wired as phase-2 Step 0 for concept-first games and before expensive operations; /find-skills (vercel-labs/skills, npx skills, 18+ agents) with selection hygiene (1000+ installs, official sources, sub-100-star scepticism) plus our rule: check the catalogue BEFORE writing a new skill, write our own only for our specifics. Both in advisor/справочник/dashboard.

- 2026-08-02: **v4.59.7** — targets come from OUTSIDE, not from own games (user: reading our own releases just repeats what we found; the point was to analyse the market). Own numbers are a CEILING — D7=6% last time → target 6-7% → design to repeat last year. Phase-1 and product-metrics rewritten: don't walk the user's projects when setting targets; sources = platform genre top (leaders, density, playtime, review complaints), external genre data/benchmarks (casual D1 30-40 / D7 10-15 / D30 3-5), 2-3 direct competitors; target = range + source + date. Own figures keep one job: Ф9 plan-vs-fact AFTER release, as measurement not ambition.

- 2026-08-02: **v4.59.6** — benchmark lookup bounded: the v4.52.0 'look at your own releases first' rule was right but unbounded, so phase-1 walked the whole park reading full metrics files. Now: max 2-3 games of the SAME genre (ask the user if unsure instead of reading everything), grep only retention lines, one-line fallback to the industry frame when data is absent, max 3-4 commands total — the phase's context belongs to the content deficit.

- 2026-08-02: **v4.59.5** — launch command restored to `cf` (scripts/cf.bat = claude --dangerously-skip-permissions, documented in GUIDE.md and installed to PATH). In v4.49.3 I rewrote the dashboard instruction with plain `claude`, so fresh projects started in manual mode. Fixed in dashboard modal, new-game.mjs hint and справочник. Lesson (3rd of this shape in 2 days): when rewriting an instruction, read what the old one said — it encoded knowledge (cf, ./ prefix, sync path) not obvious from the code.

- 2026-08-02: **v4.59.4** — dashboard instructions failed in PowerShell (it won't run files from the current dir without a path); all bat calls now use ./ (forward slash — no escaping; the backslash attempt produced four through HTML-in-JS layers). Second bug found: dashboard and справочник pointed at scripts\\sync.bat while the wrapper lives in the engine ROOT — following that gave 'file not found' and no sync. Lesson (twice in two days): a command in documentation isn't delivered until it's run in the target shell.

- 2026-08-02: **v4.59.3** — Windows compatibility: new-game.bat crashed on spawnSync /bin/bash ENOENT after successfully creating the project (my `ls | wc -l` verification). Audited all scripts/hooks for unix-only deps: new-game now uses readdirSync().length; asset-scan lists .zip via PowerShell System.IO.Compression on Windows (no unpacking) with tar -tzf fallback for .unitypackage, unix path kept for Linux. Lesson: a shell pipeline in a shipped script is a hidden platform assumption — test as Windows commands, not just parse.

- 2026-08-02: **v4.59.2** — rejection batch analysed: 2.14+1.19 = ONE load-order bug (same attachment); **3.5 platform brand in game texts** — developer licence grants NO right to Yandex trademarks/names/logos, so SDK integration ≠ licence to print the name (yandex-sdk-integration ™️ section with replacement table, auth/payment dialogs are drawn BY the platform, same for RuStore/VK, caveat that 3.5 with attachment is usually about ASSETS); 1.8 touch targets; 4.5.1 bare number on RV button reads as reward size but means remaining uses → reward in words on button, remainder on a separate line. debugcheck v2.23: brand-word, touch-target and RV-label checks (119 total, fixtures clean).

- 2026-08-02: **v4.59.1** — audit fixes vs current Claude Code plugin spec. MCP server: verifiers now run with `cwd: FORGE_PATH` (were scanning caller's cwd — wrong dir when server launched outside Forge root); test count unpinned (>= 18), 20/20. Advisor: phantom /build row removed (verbatim dup of /phase-3-construct from the rename), `build` trigger moved into phase-3-construct. Plugin-install parity: approval-gate.mjs added to plugin-hooks.json, `commands` declared in plugin.json (default scan is `commands/` at plugin root — /app /game /do /continue silently didn't load via /plugin install). marketplace.json counts refreshed (133 skills, 22 validators).
- 2026-08-01: **v4.59.0** — NEW screens-shoot.mjs + mandatory SELF-REVIEW in ui-review. Discovery: making the executor shoot and GRADE its own screens beat every textual rule — it returned 4/3/5/7/4 out of 10, named the structural cause itself ('screens built as DOM lists with no pixel of art') and found 3 defects incl. battle at 1.23 mobile screens. Script: serves game, walks states by button text, shoots mobile 412 + desktop 1920, flags non-fitting screens, builds contact sheet. ui-review §самооценка: grade format, honesty rules (grade the FRAME not the intent; state % empty and whether ANY art), <6/10 = back to work before showing the user, low marks everywhere but one = structural cause, re-shoot after fixes. Mandatory in Ф4 and Ф7.

- 2026-08-01: **v4.58.0** — design process adopted from Anthropic's frontend-design skill (user: 'Claude Design must be built on skills — where are they?' — correct, they ship frontend-design/canvas-design/theme-factory/brand-guidelines). Into art-direction: calibration against our own AI defaults (system font, 1px borders, grey 8px panels, emoji icons, tables, purple-blue fantasy gradient), TWO PASSES (plan with 4-6 named hex + typefaces by role + ASCII wireframe + SIGNATURE element → self-critique 'would I produce this for any game of this genre?' → then code), draw from the game's own world materials, spend boldness in one place (Chanel rule), copy as design material (active voice, button=result naming, errors don't apologise). visual-upgrade: realise the plan, missing SIGNATURE = visual not delivered.

- 2026-08-01: **v4.57.0** — NEW /screen-flow (user's diagnosis of Спорное поле, sharper than mine: meta isn't excessive, it's UNSEEN — everything on one screen behind tabs; tabs are a filing cabinet, not entry points; also why the tutorial had nowhere to lead). We had ui-pipeline for one screen, nothing for the screen GRAPH. Canonical cycle base→map→battle→result→base with what each screen owes the player, mandatory Ф2 table, where daily/shop/chests/clans live on the route, 5 symptoms of broken architecture, cheap repair for existing games (split screen into states + result screen, hours not rewrite). Ф2 designs, Ф4 composes each screen from the schema.

- 2026-08-01: **v4.56.0** — NEW scripts/gap-audit.mjs: mechanical audit of an existing game against current doctrines with priorities and time estimates (🔴 blockers: e.key keyboard, no input gate before ready, dev tools in build; 🟠 money/retention: RV count and variety, gacha pity, missing/decorative tutorial, no metrics.md; 🟡 quality: no refs board, unapproved etalons, system font, no plan-vs-fact; 🟢 dimensionality). Enables triage across 18 games instead of impossible full retrofit. Справочник/dashboard/advisor wired; tested on fixture.

- 2026-08-01: **v4.55.0** — approval gate: STOP-point made a LOCK. Field case: executor collected 78 real reference screens and correct style bibles (even measured icon legibility at 38/43/50px and greyscale) then showed the user NOTHING — generated on its own, cancelled its own work, burned money. NEW PreToolUse hook approval-gate.mjs blocks mass-generation commands when assets/bible exists without selection.json, and when variants were added after approval; tested on 4 scenarios; documented escape hatch. CORE rule: stop-point is a stop not a paragraph; gathered material must be SHOWN; approved decisions never redone silently; mass ops state volume/cost first.

- 2026-08-01: **v4.54.1** — audit #5 of everything added today: new skills wired into the right phases, core doctrines in place, ZERO orphan skills, advisor knew 9/10 topics. Three gaps closed: Ф3 didn't know use-template (still said 'copy the profile'), Ф1 didn't reference the target-number-origin rule (now requires a source per number + look at previous games' plan-vs-fact first), advisor lacked the two-storey retention symptom. Method note: escaped-alternation greps produced false negatives — every ✗ re-verified against the file.

- 2026-08-01: **v4.54.0** — NEW /store-creatives (icons/promo were generated 'just to exist'). Research: Yandex Console HAS built-in A/B for icons+covers (audience targeting, selective rollout) and version experiments; we already export ctr_icon/ctr_cover/conversion_to_play and never read them. Skill: creative must read genre in 1s + evoke emotion + not deceive + survive 100px + not duplicate the icon; every variant made for a WRITTEN hypothesis, bible runner-ups kept as A/B candidates; A/B procedure incl. ONE-moderation-at-a-time constraint (test blocks updates), 15-min withdrawal, no verdict before a week, watch retention after click; symptom table for reading results into wiki/decisions. Ф4 creates with hypotheses, Ф9 measures + runs A/B.

- 2026-08-01: **v4.53.0** — NEW /game-tutorial (onboarding existed only for apps; phases 3/7 never mentioned tutorials). Seven rules: one action per step (UI inert until done), HIGHLIGHT via dimming mask + cut-out (clip-path / destination-out) + pulsing outline + pointer + line next to target, clicks only through the cut-out; text <=8 words imperative, learn by doing, lore after first win; script as DATA in tutorial.json tied to phase-2 first-60-seconds storyboard; contextual hints on first encounter, shown once, saved; skip from step 2-3 without breaking the game + replay from settings + progress in save; the tutorial IS the game (real fight, real reward). Ф3 builds, Ф7 verifies (newcomer pass, click-outside inert, skip safe, mid-restart resumes, what player understood at 5/20/60s).

- 2026-08-01: **v4.52.0** — proactive audit for the 'produced from memory' CLASS (after the reference-board root cause). Four holes: (1) target metrics invented → product-metrics 🎯 (genre top + own past games as benchmark, ranges with origin, revised against reality); (2) NO plan-vs-fact loop → phase-9 📊 mandatory D7/D30 table, >30% delta = wrong retention model into wiki/decisions, facts become next game's Ф1 benchmark (closes Ф9→Ф1); (3) listing written blind → phase-6 👀 look at category top-20 (icon differentiation, name patterns + 5.12 uniqueness BEFORE rejection, first description lines); (4) first 60 seconds never designed → phase-2 ⏱️ second-by-second storyboard, verified in Ф7. CORE rule added: produce from a SAMPLE not from memory, and say out loud when there's nothing to compare with.

- 2026-08-01: **v4.51.0** — reference board in art-direction (third '90s interface' complaint; root cause: style spec written from MEMORY, and model memory of 'game UI' = system fonts + default borders + tables). Step 0: named pro sources (Game UI Database, Interface In Game, ArtStation, itch/store genre top), 4-6 categories, 3-4 downloaded refs each into assets/refs with mandatory source attribution, board built, work STOPS for 🔴 direction choice, spec written FROM chosen refs (palette picked off them, density/composition described from them). Boundary: inspiration not copying. asset-bible.mjs generalised (--dir/--title) to serve both boards.

- 2026-08-01: **v4.50.0** — 📖 asset bible before mass generation (user request): NEW scripts/asset-bible.mjs + asset-generation Step 4.2 MANDATORY — 4-6 categories, 2-3 DELIBERATELY different variants each, library candidates as lib-* in the same folders (compare generated vs ready in one place), prompts in _prompts.json for reproducibility, page with click-to-select + progress bar → selection.json; executor STOPS for the 🔴 style decision. No selection.json = no mass generation; approved reference feeds generation as image reference (up to 15). Phase-4/справочник/dashboard wired. Tested on fixture.

- 2026-08-01: **v4.49.3** — dashboard still taught forge.ps1 new + sync.ps1 in the project-creation modal (v4.44.0 updated skills/docs but not the UI). Modal now: new-game.bat <slug> → GameIntegration → claude + /phase-1-analyze . → sync.bat; copy-button, command cards, tips and sync-preview (sync.bat --dry) updated. Lesson: a change ships only when every teaching surface is updated — skills, docs AND dashboard.

- 2026-08-01: **v4.49.2** — sync.mjs prunes orphan command wrappers: the v4.28.1 revert removed them from the engine but never from games, leaving 115 dead slash entries per project for weeks (surfaced by the deprecated sync.ps1 warning: 119 vs 4). pruneOrphans() removes .claude/commands files absent from the engine (dirs untouched, --dry respected), report shows 'убрано сирот: M'. Verified: 115 pruned, 4 canonical intact. Skills not pruned (games may have own).

- 2026-08-01: **v4.49.1** — ui-pipeline/ui-review were ORPHANS (existed, referenced by no phase — same class as audit #4 agents; that's why Ф4 never designed screen composition). Wired as Ф4 step 2 (mandatory beyond a single HUD: decides what is PRIMARY/secondary/hidden) with /ui-review acceptance on a 1920x1080 screenshot; advisor symptom line added. No tenth phase: UI is a cross-phase property (interaction Ф3, composition Ф4, onboarding Ф2).

- 2026-08-01: **v4.49.0** — field case (game 'ready' looking like an admin panel: tables not scene, system fonts, default borders, dev tools in build, executor refused library assets and offered nothing). Ф4 Step 0 can't end in 'nothing fit' — must propose generation as a concrete list with counts; forbidden outcome spelled out (default CSS/system fonts/emoji/tables). visual-upgrade += «БРАУЗЕРКА 90-х» test (7-row defect table + verdict question). debugcheck v2.22: static check for leftover dev tools → WARN per 1.15.

- 2026-08-01: **v4.48.2** — numeric thresholds rule in core: a threshold without a reference to a platform requirement is a HYPOTHESIS, not a constraint (field: executor about to cut the game for a '<=60 KB bundle' GDD line while the real limit is 100 MB/archive and the target was a stale prototype estimate). Outdated hypothesis → propose revision as 🔴 block + wiki/decisions, don't mutilate the game; phrase budgets as USER EFFECT (TTI on a weak phone, 60 FPS) not internal units; real platform limits stay untouchable. Ф2 requires a source next to every GDD threshold; advisor got the symptom line.

- 2026-08-01: **v4.48.1** — templates reachable from games via NEW scripts/use-template.mjs (pull-on-demand, --list, never overwrites existing files, copy belongs to the game). Downstream had proposed adding templates\ to sync.ps1 payload — right diagnosis (/multiplayer couldn't find backend/async), wrong remedy: sync.ps1 deprecated since v4.44.0 and mass distribution would copy backend scaffolding into 13 games and create drift. /multiplayer + backend-builder now pull explicitly. Tested: 9 files into a fresh game.

- 2026-08-01: **v4.48.0** — model selection per phase by VERIFICATION density: opus where machines can't check and errors cost weeks (Ф1 deficit, Ф2 GDD/economy, Ф3 complex logic, art-direction, rejection analysis, engine work); sonnet where a checker/script/screenshot verifies (Ф4-Ф9, asset-scan, promo, ports, routine /do) — the verification layer is what makes the cheaper model safe; haiku not for the pipeline. Shipped: Модель line in all 9 phase skills, core rule, справочник table, dashboard badges.

- 2026-08-01: **v4.47.2** — Unity packs rehabilitated (executors skipped them for HTML5, reading kind:unity as Unity-only — our own 'engine specifics are useless' wording over-generalised; 329 of 1347 sources were invisible). .unitypackage = tar.gz with ordinary FBX/PNG/WAV, usable in web after conversion and often the BEST source for 3D. asset-library += 📦 section (take/leave table, extraction sketch, 2D games benefit too → use:any), phase-4 warns not to filter kind:unity, asset-find prints an inline hint on Unity results. Verified on the real library.

- 2026-08-01: **v4.47.1** — audit #4 (connectivity). Fixed: Ф9 had no next-phase (now closes the LOOP: repeat on schedule / metrics down → Ф2+Ф3 / stable → port / content exhausted → Ф1 next game); Ф6 didn't name record-promo; advisor lacked hybrid monetization+RuStore-first, two-storey retention, dimensionality, asset-find/scan, backup-data; FIVE orphan agents wired (qa-tester→test-game, security-auditor→phase-8 secrets/endpoints/purchase validation as release blockers, platform builders→/port). Verified: chain 1→9, hooks parse + 4 events registered, advisor catalog in sync, wiki discipline in every phase.

- 2026-08-01: **v4.47.0** — game dimensionality established in phase 1 (was never asked anywhere while phase 4 already required it — silently defaulted to 2D). Ф1: detect BY FACT from prototype (three./WebGLRenderer/getContext) or 🔴 user decision 2D/2.5D/3D with tradeoffs; result recorded in wiki/_map.md as «Размерность». Ф2 consults it before the content budget (changes it several-fold), Ф4 uses it for the library filter and three-setup branch, Ф5 gained a dimensionality-keyed performance budget (3D → mandatory weak-Android check).

- 2026-08-01: **v4.46.0** — hybrid monetization for long-progress games (Vikings-class analysis): payments = PROGRESS, ads = HABIT; RV never equals a purchase, mandatory RV daily limits, p2w red line (speed not invulnerability, seasonal resets), starter pack for first conversion, no ads for payers, health marker (ads >50% of revenue = progression underpriced). /port += RuStore-first exception (IAP games invert Yandex-first; ИП/юрлицо confirmed BEFORE start). phase-2 asks the model FIRST (🔴 ads-only vs hybrid, genre rule: minutes → ads, hours/days → hybrid). rustore-builder += IAP procedure; async backend += purchases table (unique receipt = idempotent), is_payer flag, /api/purchase/validate with validateReceipt defaulting to FALSE.

- 2026-08-01: **v4.45.0** — two-storey retention for social games, from Hired Heroes field data (first 3 days players ignore clans entirely; clan-vs-clan WARS drove 3-4h sessions). product-metrics: storey 1 D1-D3 single-player (population-independent, solo-reachable, mostly TEXT content budget), storey 2 from D5-D7 clan driven by CONFLICT; critical mass needed at the funnel's narrow point not the entrance (~500 at D7 = 20-30 clans); don't smear social features into days 1-3; KEY metric = share joining a clan by D7 (more diagnostic than D7); clan wars = LiveOps obligation stated in the 🔴 decision. Retention table += storey rows. phase-2 multiplayer decision references it.

- 2026-08-01: **v4.44.0** — one way to create a project: NEW new-game.mjs/new-game.bat (plain sibling folder + skeleton + sync.mjs delivery + verification, tested: 130 skills on first run). Root cause of 'projects come out broken': forge.ps1 new used git worktree + junction to skills\ while Claude Code reads .claude\skills\; sync.ps1 was a different generation. Both now print deprecation warnings pointing at the canonical commands.

- 2026-07-31: **v4.43.3** — NEW scripts/asset-find.mjs: library search from any folder (resolution ./forge-data → ../forge-data → engine/../forge-data → engine legacy), filters kind/use/lic, ranks by tag hits + user rating, prints only matches (+ unconfirmed-licence warning), --json. Library is 1.2MB — reading it into context is not an option. Skill + phase-4 Step 0 now call the command; справочник row. Tested from engine and sibling game folder on the real 1347-source library.

- 2026-07-31: **v4.43.2** — library reached 1347 sources / 1843 tags / 1301 with extraction notes; agents had invented 38 kind values vs the allowed 6 (records invisible to filters). NEW scripts/asset-normalize.mjs collapses kind/use/lic to the vocabulary and preserves originals as tags (--dry preview); ran on the real library: 164 fixed, distribution clean. Agent + skill now enforce the enum and require a normalize pass after merge.

- 2026-07-31: **v4.43.1** — classifyBash false positives upstreamed (field: grep with 'deploy' in the pattern and a node -e one-liner both tagged deploy → stop-hook demanded a deploy-log entry for a non-existent deploy; downstream correctly refused to fake the entry). Classifier now decides by FIRST WORD: read-only commands and node/python -e one-liners and sed -n → shell; deploy only for rsync/scp/rclone as the command, npm run deploy, or a deploy script invoked directly. Unit-tested on 12 cases incl. git commit -m 'deploy fix'.

- 2026-07-31: **v4.43.0** — parallel asset research (fan-out/fan-in): asset-shard.mjs splits draft into up to 20 shards; NEW agent asset-researcher (sonnet+web) enriches ONE shard and writes shard-NN.done.json, never touching the shared library (no write races); asset-merge.mjs dedups by path, prefers the fuller record, ALWAYS preserves user rating/verdict, writes via data-dir with auto-backup, reports counts + unfinished shards + unconfirmed licences, --dry preview. Skill: parallel mode mandatory from ~40 packs, dispatch all agents in ONE message. Tested end-to-end with a pre-existing ★5 rating — preserved.

- 2026-07-31: **v4.42.2** — ROOT CAUSE of the data loss: not update-forge (extracts with -Force) but the engine's own apply-manifest orphan sweep, which deletes anything not in MANIFEST.txt — and v4.40.1 had removed asset-library.json from the distribution. Fixed 3-deep: USER_DATA protected in apply-manifest + upgrade.ps1 (which now MOVES stray root json to ../forge-data instead of deleting), session-start takes a daily snapshot into forge-data/backups, update-forge.bat runs backup-data.mjs as step 0 and ABORTS if it fails. Lesson: moving a file out of a distribution exposes it to every disk-vs-distribution sweeper.

- 2026-07-31: **v4.42.1** — DATA LOSS incident fixed at root: user's asset-library.json (294 sources) was WIPED by an engine update (update installs as clean folder replacement — not shipping a file makes it MORE vulnerable, not less). User data now lives in ../forge-data/ (sibling, untouched by updates) via NEW scripts/data-dir.mjs (user-first resolve, writeData with 10-version auto-backup, migrateLegacy) + NEW scripts/backup-data.mjs (pre-update snapshot + explicit warning if user files still sit inside the engine). Core rule rewritten. Tested on mock layout.

- 2026-07-31: **v4.42.0** — asset library: localStorage removed entirely (user's call after the file:// quota case). Model = load json → edit in memory → save json into engine root; contextual banner (not loaded / loaded N from file / unsaved edits) + dirty flag with beforeunload prompt; skills reworded (file = single source of truth, page owns nothing). Verified end-to-end on the real 294-entry library.

- 2026-07-31: **v4.41.3** — asset library works from the FILE: user's 294-entry/236KB library was clean, but Chrome gives file:// pages a tiny opaque-origin storage budget. Page now probes storage, falls back to FILE MODE (memory + banner + dirty tracking + beforeunload prompt, export clears dirty); localStorage optional. Verified in a simulated storage-denied environment.

- 2026-07-31: **v4.41.2** — asset library at scale (first big Humble scan: localStorage quota exceeded + 12s freeze). Fixed over-descent (container needs >=3 candidates, 0 own assets, no structural subfolder names; depth 2; --max-packs 400 with warning), bloat (import slims to 11 library fields, truncates long notes/desc; skill lists allowed fields — draft fields never enter the library), quota/freeze (guarded save reports size in MB and keeps the json intact; grid caps at 200 cards + 'показано N из M'; header count). Skill += granularity rule: one entry = one pack used as a whole.

- 2026-07-31: **v4.41.1** — multiplayer ownership: phase-2-design asks the multiplayer question as a 🔴 DECISION (no / async / sync, with genre-based recommendation — server = recurring cost); NEW agent backend-builder (deploys profile, .env with Console secret, docker compose or exact commands when no SSH — simulating a deploy forbidden, wires mp-client, surrenders by 3 facts: health ok, forged signature 401, game playable with backend down, secret not in build); phase-3-construct system sprint += backend deployment, turn logic server-side only; /multiplayer skill opens with a roles table.

- 2026-07-31: **v4.41.0** — multiplayer part 1: ASYNC profile (templates/backend/async: Caddy TLS + Fastify + Postgres; players/clans/actions/scores; intent-in result-out action endpoint; clan feed since-cursor; own leaderboard; per-player rate limit; platform-only CORS) + mp-client.js + /multiplayer skill (profile choice, iron rules: identity ONLY from verified Yandex signature (HMAC-SHA256, unit-tested valid vs forged), intent-not-result, https/wss, game playable with server DOWN, secret never in build, RuStore account scheme decided pre-release, cost check). Yandex confirmed: no platform multiplayer tooling. Next: SYNC (Colyseus).

- 2026-07-31: **v4.40.2** — asset-scan containers + duplicates (user scanned sub-folder then root). Directory with >=2 pack candidates and no own assets = CONTAINER → descend (3 levels), so bundle roots no longer register as one swallowing pack. Scanner reads user's asset-library.json and flags: already_in_library exact / overlap:<path> (nesting), same_name_elsewhere, dup_in_scan + console summary. Skill Step 3.5: children beat parent entry, versions disambiguated, nothing deleted silently, scan the collection ROOT. Verified on nested fixture.

- 2026-07-31: **v4.40.1** — user data survives updates: asset-library.json shipped in the engine zip would have been overwritten by update-forge; shipped sample renamed asset-library.seed.json, user file never ships, skills read user-first with seed fallback, asset-scan creates from seed, HTML states it + json = backup. CORE rule added: hand-edited user files never ship under their own name (<name>.seed.* pattern).

- 2026-07-31: **v4.40.0** — /asset-scan: point at a folder of asset packs → tagged library entries. scripts/asset-scan.mjs = facts (archive listing without unpacking, .unitypackage pathnames from tar.gz, extension histogram, kind guess, sizes, samples → draft json); skill = judgement+internet (research only unclear packs, tags as SEARCH WORDS, use axis, extraction notes, licence class with 'unconfirmed → no + verify at vendor', merge into asset-library.json deduped by path preserving user rating/verdict, report with warning lists). Guards: >50 packs split, never unpack, never invent contents/ratings.

- 2026-07-31: **v4.39.1** — asset library refinements: `use` axis (2d/3d/any + filter + badge; 3D→sprites = any), quality RATINGS 1-5★ on cards with sort/hide-weak and skill rules (prefer ≥4, never offer 1-2 except last resort; Forge proposes a rating with reasoning after use → library self-cleans), phase-4-visual Шаг 0 = explicit user question (library / generate / mixed) with matches listed by rating.

- 2026-07-31: **v4.39.0** — asset library: NEW asset-library.html (engine root, dashboard button) for user-curated sources (name/path/kind/desc/tags/license+date/extraction notes, filter, export → asset-library.json, import) + NEW /asset-library skill (search library BEFORE generating generic assets; license gate free/attr/paid/NO-nc-gpl; extract per source notes; art-direction style gate; provenance in wiki/assets-provenance.md) + asset-generation Step 4.0 + dashboard/справочник rows. Seeded with the Synty Unity-pack entry (catalog-first, selective extract, Blender-only, gltf-transform, never unpack whole library).

- 2026-07-31: **v4.38.1** — debugcheck v2.21: Event Timeline verdicts the order (click-vs-ready REQ-1.19, lang-vs-click REQ-2.14, lang-vs-ready, ready-vs-init) with ORDER OK/FAIL badge; panel previously printed expected order as a hint and never checked it (same 'data collected, pair not compared' pattern as the rejection). Verified by replaying real field timings: ORDER FAIL, 2 red.

- 2026-07-31: **v4.38.0** — rejection 1.19 (app-553975: input at 1143ms vs ready() at 2535ms, lang at 2837ms — playable before GRA). debugcheck v2.20: runtime FAIL «ВВОД ПРИНЯТ ДО ready()» + static input-gate check; yandex-sdk-integration: hard order init→detectLang→paint→ready()→inputEnabled=true, gate snippet mandatory, no press-to-start before ready, no 'picture alive/ready pending' state. Checker had both marks but never compared click-vs-ready.

- 2026-07-26: **v4.37.1** — localize frontmatter repair: v4.33.0 prepended the RU-only modes block ABOVE the --- header, breaking name/kind/description (skill may not register); slipped 4 versions because that release ran only check-drift, not the full suite. Frontmatter restored, description rewritten from stale '13 языков' to RU-only two-mode wording. Lesson: the RELEASE GATE is the full verifier suite, not one guard.

- 2026-07-26: **v4.37.0** — quarterly sweep implemented. AUDIT: 83/127 skills had descriptions >250 chars (silently truncated triggers pre-July-2026 cap raise to 1536; max 835 → all healed) + NEW check-skill-descriptions.mjs wired into drift. $1 placeholders documented in phase-1/pipeline/port/localize. asset-generation +Step 4.6 pixel-art pipeline (generate large→downscale→re-index palette→clean; AI can't hold animation frames — one base + code animation; tiling-aware tilesets; Flux 2 / Z Image Turbo noted) +Step 4.7. NEW scripts/asset-catalog.mjs (one-page visual catalog: palettes, dims, filter — whole style at a glance) + dashboard/справочник rows. Yandex signals: playtime →60min, 24k published vs 29k REMOVED in 2025, WebGPU coming, AdMob gone from RF.

- 2026-07-22: **v4.36.0** — canonical sync layer (metro case: new folder had no skills; sync.bat never existed in engine). scripts/sync.mjs + sync.bat: auto-discover siblings, payload skills/agents/hooks/AGENTS/справочник/debugcheck, byte-compare idempotent, facts per game, --game/--dry; new-game.bat = mkdir + targeted sync. Smoke-tested (188 files, then 'актуально').

- 2026-07-22: **v4.35.0** — pack-handoff.mjs: one-command tracker archive (newest build zip across game tree + ../Release/<name>; store materials by pattern: SETUP_GUIDE, listings, icon/cover, screens+video, StoreData, metrics; MANIFEST with sizes/mtime as facts; <game>/handoff/<name>-handoff-<date>.zip; explicit warn when build missing). Dashboard PH_DATA += Handoff row (copy in panel+modal), справочник += row, baseline regen. Smoke-tested.

- 2026-07-21: **v4.34.2** — modal checklist fixed (global .modal input{width:100%} inflated checkboxes → staircase; explicit inline sizing + flex rows) and per-phase ⧉C/⧉K copy buttons added in the modal. Lesson: injected components must neutralize container's global CSS.

- 2026-07-21: **v4.34.1** — phase checkboxes moved into edit-project modal (two columns: fields left, ФАЗЫ КОНВЕЙЕРА right, phases 1-9 + port; modal 880px on open, same forge_phases_<name> storage). Bottom panel = pure prompt reference (selector/checkbox column removed). Baseline regenerated, guards green.

- 2026-07-21: **v4.34.0** — dashboard «🏭 КОНВЕЙЕР ФАЗ»: game selector (forge_projects), 9 phases + utils, per-game done-checkboxes (localStorage forge_phases_<name>), ⧉ copy for Claude command and FULL Codex/Kimi prompt per row. Maintenance enforced: new drift-guard — every skills/phase-* dir must appear in dashboard.html (rename/add without PH_DATA update = red drift). Baseline regenerated.

- 2026-07-21: **v4.33.4** — Console pre-publication checklist encoded (user screenshot): fill-yandex item 14 mirrors the real list verbatim w/ paragraph refs and our-check mapping; NEW §1.14 auto tech-gate 'draft was launched' → phase-8 gate 0a + MANUAL line with date; NEW mandatory AI-usage field → 14б 🔴 decision, honesty rule (conveyor = AI tooling: 'частично' when user drives design/decisions, 'полностью' for end-to-end), one studio-wide answer in wiki.

- 2026-07-21: **v4.33.3** — Step 0.7 rule 2c (Обход round 2): desktop = RECOMPOSITION of the core, not margin decoration. Fixed portrait core scaled by height leaves center narrow by construction. Desktop core own width (>=900 vs ~420), content 2-3 rows across, sprites larger, fonts 16-18/22-24, modals from desktop core w/ full-screen dim + padding, no center/rail duplicates, pointerover tooltips mandatory, half-empty HUD = defect.

- 2026-07-20: **v4.33.2** — Step 0.7 +2b composition (field case Обход: formal grid compliance = three floating default-CSS boxes, worse than void). Panels grow FROM field edge (one scene one frame), built from GAME materials (palette/textures; default border = defect), center grows too (~20-25% panels, field the rest), backdrop = art-direction atmosphere. Test: one artwork or game+admin-bolted-on?

- 2026-07-20: **v4.33.1** — ad-rules REQ-3.8 false positive upstreamed (downstream patched locally 5x, engine updates kept overwriting): \\d[\\d.,]* matched numbers ending in comma/dot → minified `-16,$=new Y` flagged as hardcoded currency. Fix in 3 places: number must end with digit. Unit-tested (299₽ caught, minification artifacts not).

- 2026-07-20: **v4.33.0** — RU-only doctrine: draft declares ru only, one Russian kit (no 13x anything), auto-translation FORBIDDEN; architecture always i18n-ready (I18N.ru dict + t(), hardcode = defect, detectLang SDK-first) so adding a language later = dict translation not surgery; /localize <lang> = explicit user command, one language per command. localize split into two modes; phase-6 rewired.

- 2026-07-20: **v4.32.4** — bump-version works from any cwd: root resolved from script location (drive-letter-safe), chdir to root, cwd fallback. Version check = first step of field prompts, must not require cd.

- 2026-07-20: **v4.32.3** — two rules from drawRival case (promo shoot found PROD freeze night 4+, v1.4-v1.8): construct += auto-replace only by UNIQUE anchor (grep -c==1, widen until unique); test-game += late-states rule (reach late waves WITH render() alive; state advancing != game working, verdict by late-stage frames).

- 2026-07-19: **v4.32.2** — self-healing rAF shim in record-promo (field round 2: CDP focus + flags STILL left rAF dead; timers alive). Shim injected BEFORE game code: queue wrapper, native ticks flush transparently, >250ms silence → watchdog flushes on timer with performance.now(). Late-spoof failure impossible; CDP kept as first line, shim = floor.

- 2026-07-19: **v4.32.1** — recorder frozen-rAF fix (field: automated browser = unfocused → rAF freezes → screencast returns first frame while JS state advances; flags/headful/visibility-spoof insufficient). CDP Emulation.setFocusEmulationEnabled wired into record-promo.mjs + anti-throttling launch flags. Fallback documented: deterministic canvas.toDataURL frame compositor → NVENC.

- 2026-07-18: **v4.32.0** — canonical scripts/record-promo.mjs (downstream started writing its OWN recorder — per-game-zoo class). Mock-SDK server + puppeteer self-play (menu/grid/touch/keys) + page.screencast + GPU-only assembly (nvenc hard-required) + trim<=28s via --trim-start + desktop&mobile profiles + ffprobe facts; raw kept for re-trim. promo-screens: recorder = primary path, one-off recorders in games FORBIDDEN, OBS = quality alternative. Справочник += row.

- 2026-07-18: **v4.31.2** — video processing GPU-only: NVENC recipe in promo-screens (hwaccel cuda + h264_nvenc p5 cq23, scale_cuda; CPU encode forbidden; nvenc availability check).

- 2026-07-18: **v4.31.1** — record-promo.mjs: gameplay promo video (limits live in Console form per 5.3, ~28s/100MB; 8.3.1 quality). Script plays the game itself (playtest heuristics) + CDP screencast → ffmpeg mp4 h264, desktop/portrait via --w/--h, no audio (autoplay muted anyway). Surrender by fact: ffprobe resolution/duration/size printed, mismatch = exit 1. promo-screens wired with commands + content rules (gameplay from sec 1, no debug panel, eyeball it).

- 2026-07-18: **v4.31.0** — phases integrate monetization/meta: Ф2 += aggressive-RV pass + /gacha-meta selection into GDD/STOP-point; Ф3 += mandatory system sprints (hooks, gacha module w/ pity/odds/dup-conversion, e.code keyboard); Ф7 += hook/gacha/ru-layout verification. Advisor += proactive monetization section (grep hooks before advising delta). Audit #3: engine healthy (verifiers PASS, copies v2.19, 15/15 agents, chain 1→9, 0 stale refs, 0 CJK, coverage=reality); one gap fixed — справочник lacked /gacha-meta row.

- 2026-07-18: **v4.30.11** — /gacha-meta: RV-fueled gacha/roulette/daily layer. 7-mechanic catalog (daily wheel, gacha banner w/ skins, calendar, scratch, timer chest, album, limited banner) with genre-fit + day-bucket mapping, pick 2-3; pools 60-70 currency / 20-25 consumables / 5-10 visual-only skins / 1-2 jackpot; MANDATORY visible pity in save, odds disclosure, dup conversion, no casino aesthetics; universal gacha.js pattern; 5-step retrofit + RV/DAU tie-in. Research: daily reset wheel = habit builder, mechanics match lifecycle stage.

- 2026-07-18: **v4.30.10** — (1) layout-independent keyboard: e.code only for controls (e.key==='w' dead in ru layout), rule in mobile-game-ui + debugcheck v2.19 static WARN (fixture clean); (2) RV differentiation rule in monetization-design: one reward type = one hook max, hook→reward table at retrofit, repeat = defect; moment coverage (pre/during/after/fail) beats button count.

- 2026-07-18: **v4.30.9** — SETUP_GUIDE hardened in fill-yandex: self-contained Russian copy-paste doc (references to store-listing JSON FORBIDDEN — every Console field as verbatim value); categories/tags picked from Yandex's REAL catalog list (invented = not in dropdown = useless), verification date stamped; mandatory 14-field structure in Console order incl. SEO-no-dup 5.11, sizes via Step 4.5, screenshot sets, IAP-tab-vs-code 1.13.6 check; per-language blocks.

- 2026-07-18: **v4.30.8** — aggressive ads-only doctrine in monetization-design: RV offer every 2-3 min, 8-pattern hook catalog on player desires, >=5 hooks/game (target 6-8); interstitial = background only (aggression lives in RV — voluntary per 4.5, hence dense is safe); boundaries: base playable, honest rewards, 4.7/4.2; metrics RV/DAU 1.0/2.5/4+. +RETROFIT procedure for existing games (inventory → desire map → top-5 delta → honest 📺 buttons → playtest each hook).

- 2026-07-18: **v4.30.7** — promo-screens 3 iron rules (field: 470px upscaled to 1920 = mush vs 8.3.1; empty no-HUD frames; single set). Native-viewport capture (1920x1080 desktop / 1080x1920 portrait / landscape; downscale ok, upscale never, squash = rejection); gameplay mid-action WITH HUD (2-sec test, no dimming/debug-panel/fake bars, 3+ frames early-mid-late); desktop+mobile sets by draft orientation + per-language shots (?lang=xx). Fact check: printed dims, upscale-origin = FAIL.

- 2026-07-18: **v4.30.6** — naming rule in core: player-facing = Russian (title, modes, buttons, items, listing), machine-facing = Latin (files, ids, save keys — cyrillic paths break bats). Russian names by default (RU market); English game title only if user approved; mixing = defect (Wave 3 → Волна 3); Latin branding = user 🔴 DECISION not default.

- 2026-07-18: **v4.30.5** — store-asset prompt recipes (hostling: icon-of-an-icon with margins/vignette; cover = tiny-hero panorama). Step 4.3 MANDATORY: icon = FULL-BLEED edge-to-edge hero close-up ~80% + no-frame negatives (store rounds corners itself); cover = hero close-up >=50% height, single focus, emotion; panoramas forbidden. Step 4.5 eyeball += full-bleed check + 200px thumbnail focus test.

- 2026-07-18: **v4.30.4** — no generated text on icons/covers/promo: asset-generation +Step 4.4 MANDATORY (every icon/cover/promo prompt ends with full no-text negative suffix; title lives in Console fields; explicit title wish = PIL overlay with real project font post-step, never generated glyphs; Step 4.5 summary += eyeball 'no text' item, lettered image fails and regenerates).

- 2026-07-18: **v4.30.3** — 🔴 DECISION PROTOCOL in CLAUDE.md core (→AGENTS.md): every user-decision = standard red block (🔴🔴🔴 header, question, options+consequences, recommendation, ⏸ halted), LAST in message, execution stops, no 'пока предположим'; defined what IS a user decision (money/scope/irreversible/invariant/platform) vs what is not (mechanics in approved plan). Phase STOP-points use same block.

- 2026-07-18: **v4.30.2** — phase-8 surrenders by ARTIFACT (field: build with 16 static FAILs uploaded after 'passed' phase 8 — GREEN was a claim). The literal TOTAL line from final release-ready must be copied into the wiki release task; fail≠0 → phase not passed. Lesson #113 applied to phases themselves.

- 2026-07-18: **v4.30.1** — field defects fixed. (1) SETUP_GUIDE never produced (user ran fill-yandex by hand): phase-6 +mandatory output-files table (ls-verified; 'скил отработал' != 'файл существует'); phase-8 +gate 0 = check-setup-guide.mjs (verifier existed, wired to NOTHING — Lesson #101 class). (2) icon_512.png contained 1024px: asset-generation +Step 4.5 MANDATORY — .bat post-processes (icon 512 resize, cover 800x470 cover-crop no stretch, screens 16:9) + verifies + PRINTS actual dims, exit 1 on mismatch; filename is not a guarantee, printed size is. Lesson #113: step completion = ARTIFACT (file/pixels), never 'step ran'.

- 2026-07-17: **v4.30.0** — phases renumbered per user: 1 analyze, 2 design, 3 CONSTRUCT, 4 visual, 5 tech, 6 listing, 7 test, 8 release, 9 live. Dirs renamed tail-first, frontmatter/titles/triggers updated, next-phase chain re-verified 1→9, status hints aligned, hotkeys: 1-9=phases, 0=/status, pipeline→utilities. Old-numbering artifacts grepped to zero outside history.

- 2026-07-17: **v4.29.1** — /construct: the missing CONSTRUCTION phase (hostling case: phases 1-6 ran, game file never touched — conveyor produced only paper, executor invented sprints as another 40-day document; user: 'так а херли ты сразу так не сделал?'). /build = implement GDD features IN CODE sprint-by-sprint, playtest after each (01≠04), no permission-asking between sprints, no new documents instead of code, no SDK/visual work. phase-2 now ends → /construct; /status +rule: GDD newer than code mtime → stage СТРОЙКА → /construct. Lesson #112: a pipeline whose phases output documents will produce documents — construction must exist explicitly, verified as CODE CHANGE.

- 2026-07-17: **v4.29.0** — ports layer revived, Yandex-first doctrine. Research: RuStore Pay SDK (unauthorized buyers, test payments; payments = ИП/юрлицо since 01.02.2026, ads-only unaffected), SDK Update (~40% non-updaters), featuring metrics installs/ARPPU/rating; VK: vk-bridge + VKWebAppOpenPayForm, re-moderation after EVERY SDK update, ~45M MAU. NEW /port skill (only after release-ready GREEN; SDK layer+wrapper+listing, never gameplay; honest degradation; order RuStore→VK→sketches). NEW rustore-builder agent (was missing), vk-builder refreshed, справочник += ports. Google/Huawei = honest sketches until first real port.

- 2026-07-16: **v4.28.1** — REVERT v4.28.0 wrappers: user's Claude Code exposes skills natively as slash commands → 123 AUTOGEN wrappers duplicated every entry in the popup. Original 'No commands match' was session cache, not registration. Deleted wrappers (4 hand-written kept), guard #14 removed, generator removed. Lesson #111: if 'restart the session' fixes the symptom, do NOT also ship the structural fix; never build a compat layer for another tool's UI without reproducing the gap on the target version first.

- 2026-07-16: **v4.28.0** — slash autocomplete: generate-commands.mjs makes a thin .claude/commands wrapper per skill (123 created, custom do.md kept; desc from frontmatter, body = execute SKILL.md + $ARGUMENTS). Root cause of 'No commands match /phase-1-analyze': popup lists only .claude/commands (4 files), skills always worked as sent text. Guard #14 commands-coverage (negative-tested). Release process += generate-commands. Users: restart Claude Code session after update (slash list built at start).

- 2026-07-16: **v4.27.2** — СПРАВОЧНИК-КОМАНД.md moved INTO the engine root, versioned: title carries Forge version (bump-version rule added — stale header = half-updated install, file says so itself); opens with §0 how Forge updates (update-forge.bat flow + manual + 'personal files never inside project' rule); then full reference: 8 phases w/ hotkeys, per-skill control, tools/scripts/agents, quick scenarios, Codex formula.

- 2026-07-16: **v4.27.1** — audit #2 (Lesson #101 protocol): engine healthy (16/16 engine verifiers, copies v2.18 identical, hooks/scripts parse, 14/14 agents tiered, phase sub-skills exist, 0 CJK, coverage=reality, MANIFEST 516). ONE finding: /status suggested OLD per-skill hotkeys while Helper moved to phase layout (Ctrl+Alt+4 would fire wrong command) — fixed: phase commands + rule 'этап N → /phase-{N+1}'. Class confirmed: every remap leaves a stale reference — grep old layout artifacts after any change.

- 2026-07-16: **v4.27.0** — phase commands: 8 thin orchestrators (/phase-1-analyze .. /phase-8-live) over existing skills (phase-2 delegates to design-pipeline; no duplication). Each runs its whole conveyor phase with sub-skill gates intact, fact-based inter-step verify, STOP-points at key decisions (content-budget deficit; features-by-day), ends with summary + next-phase command. Granularity ladder: skill → phase → /pipeline.

- 2026-07-16: **v4.26.2** — product-metrics +mandatory content budget (tyl: exemplary KPI describing ONE 11-min campaign next to D7/D30 targets requiring weeks; gap uncounted). Day-bucket table: target → content the target IMPLIES → hours → exists → DEFICIT (=ТЗ for game-design Step 0.5); min horizon 7 days; KPI without content budget = metrics of non-existent game, doc INCOMPLETE. Lesson #110: targets and content live in different units — without a forced conversion table, perfect KPI + 10-minute game both look done.

- 2026-07-16: **v4.26.1** — game-design Step 0.5 MANDATORY: GDD computed FROM retention targets, not prototype description (tyl GDD fact: 311 lines, 0 mentions of D7/D30, self-titled 'по состоянию прототипа'). Hard stop without metrics.md; mandatory «Математика удержания» day-bucket table (D0-1/D2-7/D8-30: target → player does → systems → content VOLUME in numbers → hours); acceptance: every target maps to features+volume, featureless target = design hole, targetless feature = cut candidate; doc status = проект к постройке. Lesson #109: a designer given a prototype documents the prototype — force the input to be targets or analyst→designer chain degrades to inventory.

- 2026-07-16: **v4.26.0** — desktop layout doctrine (screens: square-in-black-void on widescreen, user asks for background every time). visual-upgrade +Step 0.7 MANDATORY: occupy width (stretch field / side panels >=1200px grid / min scale <=2:1), letterbox = atmosphere never flat black (palette radial-gradient + themed inline-SVG pattern + vignette; generated backdrop better), eyeball test 1920x1080. debugcheck v2.18: static WARN centered fixed-width stage + pure-black body no gradient/pattern (narrow, full-viewport N/A, fixture clean) — pairs with runtime Canvas-fills-screen FAIL. Lesson #108: recurring manual asks are engine gaps in disguise — same fix on every game means the generating template is the bug.

- 2026-07-16: **v4.25.1** — advisor awareness gap closed. Fact-check of 'в курсе всего?': catalog auto-covers skills only; playtest.mjs, local-stage.mjs, moderation-auditor parallel audits, Codex layer, update-forge.bat were INVISIBLE to advisor. Added 'вне каталога' section with when-to-suggest rules. Lesson: auto-generated catalog = illusion of full awareness; anything outside its source needs an explicit registry.

- 2026-07-16: **v4.25.0** — local-stage: панель Яндекса локально. scripts/local-stage.mjs: serves game + injects mock-SDK before game code + ?debug-mode=16. HUMAN: play in browser, panel live, no Console upload. AI (--ai --play): headless, dump window.RT to stage-out/rt.json + screenshot, exit=errors (_i18nRead/readyCalled as machine facts). Mock: i18n.lang via getter (v2.17 instrumentation works), ready/Gameplay, adv fake overlay, saves. test-game +ЭТАП 1.6. Honest boundary: not a draft replacement, fast pre-upload loop. Lesson #107: reproduce platform CONTEXT locally (mock host API), do not reimplement checks — one checker, two hosts.

- 2026-07-16: **v4.24.1** — tyl "I18n is not used" root-caused to OUR localize template: detectLang checked ?lang= URL param FIRST with early return, Yandex ALWAYS appends &lang= → SDK read = dead code on platform; static presence ≠ runtime use. Fixed template (SDK first, URL = dev-only fallback); debugcheck v2.17: runtime instrumentation (getter on environment.i18n.lang → RT._i18nRead, FAIL if never read, N/A if frozen), static URL-before-SDK order check (fires on tyl, clean fixture), legacy Sound-toggle aligned with 6.2 (contradicting verdicts in one report). Lesson #106: grep verifies EXISTENCE not EXECUTION — instrument runtime facts like the platform does; platform-injected params kill "override-first" branches below them.

- 2026-07-15: **v4.24.0** — Active playtest. Gap: runtime-test drives Chrome but checks MODERATION; smoke-test.mjs passive (0 clicks). NEW scripts/playtest.mjs (same puppeteer+autoinstall): clicks start buttons (3 menus deep, RU/EN heuristic), plays via 5x5 jittered canvas grid + --keys, 4 screenshots, collects errors DURING play, rAF-alive check; exit 0/1 + report.json. test-game +ЭТАП 1.5 with mandatory "look at screenshots with eyes" (01≈04 no diff = dead input at 0 errors). Lesson #105: passive smoke = false confidence; playtest must ACT and produce eyeball-able artifacts — "plays wrong, throws nothing" is the most common shipped-broken state.

- 2026-07-15: **v4.23.2** — Suno template fixed (vocals-in-instrumentals bug). Root cause: single-blob template with PROSE in [Structure] — Suno has 3 separate fields; anything in Lyrics not a [tag] or (parenthetical) IS SUNG. Rewritten to 3-field v5.5: Style=[Instrumental] first + genre-first order + numeric BPM + [No Vocals] last; Lyrics=ONLY [Section]+(notes), [Minimal Variation] for loops; Exclude Styles=dedicated field, plain comma list no "no", 2-3 items; contradiction check. Lesson #104: generator targeting external tool must mirror the tool INPUT FIELDS — a format that merely reads well gets parsed by the tool rules, not author intent.

- 2026-07-15: **v4.23.1** — /status rewritten FACT-BASED (user: много проектов, через неделю не помнишь этап). Old version counted wiki/_map.md items (notes go stale). New: stage from machine-checkable facts (artifacts + code greps per conveyor phase 0-6: analysis/metrics, design docs, art spec, viewport/touch, YaGames.init/ads/ready, detectLang/listing, debugcheck-version-in-build vs canonical + Release zips); wiki secondary with staleness flag; output = ✅/⏳/❌ checklist + current stage = first gap + EXACT next commands with hotkey numbers. Fact beats note (wiki says done, grep says no → trust grep, flag mismatch). Bare prototype → stage 0 → /analyze-project.

- 2026-07-15: **v4.23.0** — Orchestration tier + Codex layer. Research: SKILL.md = open cross-agent standard (our 116 skills already Codex-compatible), AGENTS.md = cross-tool instruction standard, Codex subagents GA (8 parallel). Found .claude/agents/ ALREADY had 13 agents; real gaps = no model: tiering (all on expensive main) + no moderation auditor. Tiered all 13 → model: sonnet; NEW moderation-auditor (sonnet, READ-ONLY, debugcheck+probes A-G+validators → structured report, never edits, never invents results, N parallel = N games). CLAUDE.md +ORCHESTRATION: delegate mechanics/keep judgment, never accept worker claim without re-running verifier. Codex: generate-agents-md.mjs → AGENTS.md from CLAUDE.md core + addendum (skills path, hooks gap + manual compensation, verifier reference); sha256 sync-hash + drift guard #13 (negative-tested). Lesson #103: "переписать под инструмент" обычно не нужно — стандарты сошлись (SKILL.md/AGENTS.md), правильный ход = тонкий ГЕНЕРИРУЕМЫЙ адаптер с drift-guard, не форк; и прежде чем строить "недостающее" — инвентаризация существующего.

- 2026-07-09: **v4.22.2** — advisor +"После релиза" section (gap: catalog ended at release commands). Advisor now proactively suggests on recent-release context: /seasonal-event (after publish + every 2-3wks, deadline = start −4-5 workdays), weekly rating-watch (2.13 red line, alert <40), /audit-requirements (monthly/on rejection), /product-metrics re-review (30d). Companion outside engine: Forge Helper start-ForgeHelper.bat (deps+admin elevation) + 3 new bindings (ctrl+alt+8 seasonal-event, +9 audit-requirements, +0 pipeline no-enter).

- 2026-07-09: **v4.22.1** — Rating-watch red-line added to product-metrics output template: 2.13 rule (rating ≤30 3wks → UNPUBLISHED, first ~2wks not counted) + alert at <40 + escalation playbook (reviews→update→/seasonal-event→paid promo) + weekly check table. Every new project born knowing the rule. Honest limit: reminder+plan, not automated monitoring (Console needs user login).

- 2026-07-09: **v4.22.0** — Engine side of July research. debugcheck v2.15→v2.16: +4 advisory checks from NEW Section 6 (6.2 sound-toggle N/A-aware, 6.3 pause N/A-aware, 6.5 title w/o "игра/game", 6.7 no exit button) — all WARN, fixture 0 false-positives; matters because 2.13 tightened (rating ≤30 3wks → UNPUBLISHED). NEW skill /seasonal-event: live calendar check (tg-канал+блог, дедлайн = старт −4-5 раб.дней), honest date-gated in-game activity (no external requests, Probe A–G stay green), Console promo text, rating red-line playbook. Lesson #102: platform recommendations become hard constraints one tier later — encode advisories as WARN the day they appear.

- 2026-07-09: **v4.21.2** — Web research: Yandex requirements doc CHANGED 2026-07-01 (our baseline was 05-05). Diff: 1.5 abolished (merged into 4.1); 1.13.6 NEW (in-game purchases must match Console IAP tab — the parkour case formalized); Section 6 "Рекомендуемые" NEW (sound toggle, pause, no console errors, no "игра/game" in title, no useless exit button, tutorial, lang icons — not moderated but affect quality/rating); 2.13 TIGHTENED (rating ≤30 for 3 weeks → UNPUBLISHED). Updated requirements-coverage.md + audit-requirements baseline. Also found traffic levers: тематические события (Акция badge; День Головоломки Jul 13-16), offline-play + cross-device sync boost recommendations, промо на главной каталога.

- 2026-06-24: **v4.21.1** — Engine self-audit (user requested). Full protocol: all 21 verifiers, multi-copy sync, invocation-path tracing, hooks parse, coverage-vs-reality diff, machine guarantees, active bug-hunt. Engine largely healthy (16/21 PASS; 4 "fails" = game-targeted checks run without target, expected). 3 REAL findings fixed: (1) check-platform-completeness perpetually failed — "release.yml workflow matrix" row expected .github/workflows/release.yml which no longer exists (GH-Actions era artifact); referenced by release skills → silently ignored every release; FIX: row N/A when .github/workflows absent, still verifies if file returns. (2) release-ready line 63 listed stale probe set (A/C/D/E, no F/G); FIX: text updated. (3) \bбля\b dead code in v2.15 profanity check (same \b-ASCII bug fixed elsewhere, one instance survived → standalone "бля" never flagged); FIX: Cyrillic-aware boundary (^|[^а-яё])бля([^а-яё]|$), verified catches "ну бля" and stays clean on "бляха"/"рубля"; fixture still 0 false-positives; copies re-synced. Lesson #101: an audit must run EVERY verifier (not just release-path ones) and diff docs against shipped reality — 2 of 3 findings were "check/doc referencing something that no longer exists", the class that never announces itself.

- 2026-06-09: **v4.21.0** — SYSTEMATIC requirements audit (user: "ни одна игра не прошла сходу, ты точно всё изучил?"). Honest answer was no — Forge built REACTIVELY (check per rejection), never a full audit. Fetched full live Yandex doc (2026-05-05), mapped EVERY point → wiki/requirements-coverage.md (AUTO/MANUAL/N/A-policy, single source of truth). ~45/65 checkable already AUTO (engine fine; games were built on OLDER versions). Closed ~10 static gaps: debugcheck v2.14→v2.15 +8 checks (1.6.1.1 fullscreen, 1.6.2.2 aspect≤2:1, 1.6.2.6 OS-keys, 1.15 WIP-text, 1.16 fake-ad, 3.9 youtube=FAIL, 4.3 ad-orientation, 8.2.4 profanity ru+en). All tested vs clean fixture (guard #10) → 0 false-positive; caught own bug: JS \b ASCII-only so profanity regex missed Cyrillic → split en-boundary + ru-stems. /release-ready now ALWAYS appends MANUAL "проверь сам" checklist (content/gameplay/quality/cross-browser/media/uniqueness) — GREEN = "all AUTO pass", never "fully cleared". Drift guard #12 = coverage map exists. Lesson #100: a reactively-built suite cannot tell you what it does NOT cover — only top-down audit vs authoritative spec can; map every req once, keep as source of truth, show manual remainder so green never overpromises.

- 2026-06-09: **v4.20.0** — Hexfront 6-flag rejection (built on earlier Forge). (2.14 lang + 1.19 ready "after game playable") = ROOT CAUSE __hexfrontBoot() at DOMContentLoaded binds canvas clicks + builds board while setLang(detectLang())/ready() are in a parallel init().then() resolving later → board interactive before lang/ready. NOT caught by Probe E (loader-visible-at-ready, different failure). NEW debugcheck v2.14 check: input bound at boot while detectLang/ready deferred into init().then() → WARN; fires on Hexfront, clean on fixture (RU-only still must order the call right). (1.10.3 overlap) = floating unit-actions panel over hex board blocks clicking cells beneath; Probe F catches off-screen not overlap → NEW Probe G: persistent opaque interactive panel covering >6% of canvas stacked above it (skips transient overlays/pointer-events:none HUD) → WARN. (1.6.2.5+1.6.1.6 audio in OS player) already caught by debugcheck v2.13; (5.11 repeated/duplicate draft text) already caught by store-listings — user build predated both. Drift guard for Probe G; fixture still 0 false-positives. Lesson #99: "fires after game playable" is an ORDERING bug not a timing-value bug — detect structurally (input at boot + lang/ready in async init chain); a RU-only game still must call detect-lang in the right order — the requirement is call sequence, not locale count.

- 2026-06-09: **v4.19.5** — Hexfront (built on recent Forge) got 6 flags = engine coverage gaps. Diagnosed each, closed 2 real gaps. (1.6.2.5 desktop + 1.6.1.6 mobile system player) ROOT: music via new Audio()+play() (HTMLAudioElement) surfaces OS media player; SFX use Web Audio but music does not. Forge only checked <video> not audio → GAP. ADDED debugcheck v2.12→v2.13 "Music via Web Audio not <audio>/new Audio" (WARN on new Audio()/<audio loop>/MediaSession); verified WARNs Hexfront, no FP on clean fixture. (2.14 lang + 1.19 ready "after playable") ROOT: rAF(render) at parse time → game plays immediately but async init().then(detectLang,ready) fires after; same class as parkour, fix-moderation guidance exists. (1.10.3 overlap) #unitactions floating menu overlaps hex cell; static detection unreliable → fix-moderation guidance (anchor to margin/dismiss-on-tap). (5.11 padding/dup) repeated chars or seo==about; Forge did not check → GAP, ADDED to store-listings (flags ---- padding + identical seo/about/how_to_play). Lesson #99: "built on latest Forge" ≠ "passes moderation" — engine catches only what it has checks for; every real rejection = a coverage gap; when a requirement covers a family (video AND audio players) check the whole family not one member.

- 2026-06-09: **v4.19.4** — wiki-audit stop-hook race fixed at root. While analyzing a prototype, stop-hook kept blocking "_map.md not updated since session started" right after editing it. Diagnosis (correct): post-tool-capture.mjs touches session log on EVERY tool call → log mtime always newer than wiki edit. Root cause: sessionStartMs() returns a FIXED session-start (parsed date:+first entry) which is race-immune, BUT its final fallback did statSync(log).mtimeMs = LIVE advancing mtime; on any log where date/entry parse missed, it fell there and re-created the exact v4.9.3 feedback loop. FIX: fallback now uses log birthtimeMs (creation=session start) else today midnight — both FIXED, never advancing mtime; also memoized sessionStartMs (once per run). Fix belongs in audit fallback, NOT disabling capture (log advancing is intended; audit must not compare vs a moving target). Lesson #98: when a fix has a robust path + fallback, the FALLBACK must honor the same invariant (compare vs fixed session-start, never a value that advances during session) — a fallback that violates it silently resurrects the original bug when the happy path does not trigger.

- 2026-06-09: **v4.19.3** — visual-upgrade skill +Step 7: cheap 2D atmosphere & juice, ported from the v4.19.2 3D recipe (user asked to improve 2D/pixel graphics too; most Yandex games here are 2D so higher-leverage). Checked existing 2D skills (pixel-art, visual-style, visual-upgrade, art-direction) to extend not duplicate. Gap: visual-upgrade had static palette/shadows/glow/particles/parallax but MISSED animated mood palette lerp (2D analog of 3D lerpEnv) + mood-tied vignette + consolidated juice. ADDED: 7a MOODS+moodAt(p) recolour whole scene from one scalar; 7b 2D distance fog (fade far parallax to fog colour); 7c CSS #vignette (not per-pixel, free); 7d game-feel juice (screen shake, hit-flash, squash/stretch, hit-stop, particle bursts, ease all); pixel-art specifics (integer scale, imageSmoothingEnabled=false, mood tint as multiply pass, dithering). Code + /do instruction; description + checklist updated. Lesson #97: a good technique recipe generalizes across renderers — when a recipe lands for one stack, check if the SAME idea lifts the others before inventing new.

- 2026-06-09: **v4.19.2** — three-setup skill +atmosphere recipe (Step 7) from a hand-tuned Three.js parkour reference the user shared. Analyzed the game's OWN code (separated from inlined r128 lib). Key finding: great look comes from CHEAP techniques not post-processing (zero composer/bloom/SSAO, Lambert mats, antialias off) — ideal for Yandex mobile+100MB. Added: mobile renderer tuning (antialias:false, pixelRatio≤1.75, sRGBColorSpace, PCF), fog matched to scene.background (seamless horizon + cull), DAY/DUSK/NIGHT palette + lerpEnv() driven by ONE scalar (the wow technique, near-free mood animation), player-following night PointLight + sun.castShadow off at night, CSS radial-gradient vignette (not WebGL post-pass). Both paste-ready code + /do instruction. ALSO fixed a real bug in our own skill: resize handler bound only window "resize" (same bug as reference + Probe F class) → added orientationchange+fullscreenchange. Lesson #96: when mining a reference, separate lib from game code first (7MB minified lib drowns greps), judge each technique for portability — keep cheap+robust, reject latent bugs (resize-only) even when the rest is excellent.

- 2026-06-09: **v4.19.1** — check-drift now fully clean (0 warnings). The persistent "/frontend-design referenced but no such skill" warning was a false alarm for many versions — frontend-design is an Anthropic BUILT-IN skill (Claude env, not shipped in Forge), so referencing it is correct. Added KNOWN_EXTERNAL_SKILLS allowlist (frontend-design, canvas-design, docx, pdf, pptx, xlsx, file-reading, etc). Verified precise: real typos (release-redy) + genuinely missing skills STILL caught, only Anthropic built-ins suppressed. Lesson #95: a perpetual non-actionable warning is noise that erodes trust in the whole check — either make it actionable or teach the checker it is expected; "1 warning, ignore it" every run is a smell, fix so green means green.

- 2026-06-09: **v4.19.0** — Probe F: REQ-1.10.1 multi-viewport overflow (recurring "обрезается при изменении размера окна" rejection). Forge tested overflow at ONE viewport; Yandex fits to 16:9 then SHRINKS window 20% per axis + tests non-16:9 ratios (1280x1024=5:4, 2560x1080=21:9, +1366x768/1920x1080/etc). runtime-test Probe F replays 6 viewports, flags REQ-1.10.1 BLOCKER if a button/text/score/ad-notice/canvas is >25% off-screen (doc's critical-clip threshold), skips intended overflow:hidden/scroll clips. Verified no false-positive on clean fixture; drift guard added; fix-moderation 1.10.1 updated with exact procedure+resolutions. Lesson #94: a responsiveness requirement is a MULTI-condition test — one viewport proves nothing about others; when rejection says "при изменении размера", replay the size changes literally (16:9→shrink 20%→odd ratios), do not approximate with one screenshot.

- 2026-06-04: **v4.18.1** — emoji-compat range gaps fixed + machine-guarded. User found 🦫1F9AB 🦘1F998 🦖1F996 slipping through holes between hand-enumerated 1F9xx sub-ranges. FIX: replaced gap-prone enumeration with gap-free range (1F992-1F9FF flagged Unicode 11+, with SAFE carve-outs 1F980-1F991 U8-9 + 1F9D0-1F9DF U10; 13.0 sub-ranges BLOCKER rest WARNING); widened face range 1F976-1F97A→1F970-1F97A which closed a SECOND gap user did not report (🥲1F972 tear-smile U13). NEW scripts/check-emoji-coverage.mjs: 15 known beyond-spec emoji MUST flag + 8 known-safe must NOT, asserted vs exported checkCodepoint() — caught the 1F972 gap during dev. Wired into check-drift guard #11. Lesson #93: hand-maintained codepoint lists ALWAYS grow gaps — either flag a whole block with safe carve-outs (gap-free by construction) or back the list with a curated probe test; enumerating "dangerous ones" is fragile, enumerating "safe exceptions" in a flagged block is robust.

- 2026-06-04: **v4.18.0** — Three.js parkour got 5 moderation flags (built on old Forge). Diagnosed each + checked Forge coverage. (1.10.1 clip + 1.6.1.3 deform) = ONE cause: resize() bound only to window "resize", NOT orientationchange/fullscreenchange → canvas keeps old size on mobile rotate/fullscreen-exit. Forge had ZERO checks → ADDED debugcheck v2.11→v2.12 "Canvas resizes on orientation change": canvas/WebGL/THREE games binding window resize WARN if no orientationchange/fullscreenchange/ResizeObserver/visualViewport. Verified WARNs on parkour, N/A on non-canvas, clean fixture still 0 false-positives (v4.17.4 guarantee held). (1.19+2.14 ready/lang "after playable") = async YGames.init() ("doesn't block game") → game playable before init finishes → guidance note. (1.13 purchases not found) = no purchase code, draft declares IAP → remove from draft (console). fix-moderation updated: 1.6.1.3/1.19.2/2.14/1.13. Lesson #92: 3D/canvas games have mobile-only failures (rotate, fullscreen-exit, DPR) invisible on desktop static load — check the resize/orientation path first.

- 2026-06-04: **v4.17.4** — Machine guarantee against debugcheck false-positives (user called out "audit done"→"found my own bugs 2h later"). Root of my process error: tested each new check only against the BUG it targets, never against a spec-compliant game — so checks whose normal state looks like a violation (keyboard 1.6.1.2, anti-gaming) cried wolf on first real build. Self-audit: of 8 checks added v2.7-2.11, exactly 2 false-FAILed (both fixed v2.11), other 6 verified PASS on live build. FIX: scripts/check-debugcheck-fixtures.mjs runs the REAL static checks (debugcheck now exports __CATS to Node, browser unaffected) against a known-CLEAN reference game (scripts/fixtures/clean-game.html); any static check returning FAIL on a clean game = false positive → exit 1. Wired into check-drift guard #10, runs every release. Also fixed t()-check to return boolean. Lesson #91: "I'll be careful" is not a control — when a bug class recurs, build the negative test that makes shipping it impossible; an audit is done when a test proves it, not when I've looked.

- 2026-06-04: **v4.17.3** — debugcheck v2.10→v2.11: fixed 2 false-FAILs I introduced. (1) Keyboard 1.6.1.2 (added v4.14.0) demanded .focus() whenever a text <input> existed — but native <input type=text> raises the keyboard on tap already; rewrote to PASS by default, FAIL only if suppressed (inputmode=none/readonly/disabled). Original negative-lookahead regex was also malformed. (2) Anti-gaming integrity (added v4.17.0) hard-FAILed on the game's leftover "150ms debugcheck-poll" comment + in-browser report showed "Not found!"; softened to WARN since runtime Probe E is the source of truth for ready-timing. Both now correct on samogonshchik (keyboard PASS, integrity WARN cosmetic). Lesson #85 on myself: a false-FAIL is as harmful as a miss; test every new check against a KNOWN-GOOD input that has the edge case, not just against the bug it targets — I shipped both checks without a clean-game negative test so both cried wolf on first real build.

- 2026-06-04: **v4.17.2** — i18n-completeness + store-listings validators now parse assignment-style I18N.lang={…} (samogonshchik pattern). v4.17.1 delegation worked: user's /release-ready ran real probes — Probe E (1.19.2) GREEN (ready timing genuinely fine, not gamed), 4.4 confirmed; only red was a validator false-positive. Both validators found blocks via ru:{…} / STRINGS_RU={…} but NOT I18N.ru={…} → false "no block / missing 277 keys / no in-game title" despite identical 325 keys + Probe C zero-leak. Added I18N.ru={ and I18N["ru"]={ patterns (LANG/STRINGS/DATA/NARRATIVE/L/T/TR/LOC/LOCALE prefixes); verified on real samogonshchik — now reads blocks, reports only genuine untranslated strings as warnings. Lesson #90: when a completeness validator reports a structure missing that you can SEE present, suspect the parser pattern-set is too narrow before the game; cross-check an independent signal (Probe C leak + key parity) to tell false-positive from real gap.

- 2026-06-04: **v4.17.1** — THE root cause found (explains genetic-lab 4.4 AND samogonshchik 1.19 together). User ran /release-ready yandex v4.17.0, report had NO Probe E result + self-contradiction (timing "OK" + "blocker #2"). Probe E was never CALLED: Probe A (4.4) + Probe E (1.19.2) live ONLY in platforms/yandex/scripts/runtime-test.mjs, but EVERY release skill (release-ready, auto-release, mvp-to-yandex, pre-submit-gate) invoked the GENERIC scripts/runtime-test.mjs which has neither. The whole Yandex release path ran the weak test forever — traps existed, nothing called them. FIX: generic runtime-test now auto-delegates Yandex builds (path has "yandex" OR index.html refs /sdk.js/YaGames) to the probe-bearing yandex copy, args passed through — any caller now runs the traps. release-ready self-contradiction (line 63 generic vs 91 yandex) fixed. check-drift guards delegation. Lesson #89: a check that exists but is never CALLED = no check; trace the invocation path from release command to probe, don't stop at "the probe exists".

- 2026-06-04: **v4.17.0** — Un-gameable GameReady + anti-gaming integrity (invariant #19). User ID'd deeper problem: debugcheck is injected into the build → VISIBLE to game editors → games get tuned to PASS THE CHECKER not meet the requirement (samogonshchik: "150ms so debugcheck poll catches it" + locally-patched debugcheck v2.10). FIXES: runtime-test Probe E (NEW) instruments LoadingAPI.ready() to capture whether a loading indicator/spinner/progress bar is STILL VISIBLE at the moment ready() fires → 1.19.2 BLOCKER (un-gameable: only passes if ready() truly fires when interactive); debugcheck v2.9→v2.10 anti-gaming check flags fixed setTimeout-before-ready + "pass debugcheck/probe" comments. Invariant #19 in CLAUDE.md: never tune a game to pass the checker, never edit injected debugcheck in a game project, checks must measure FACTS not code shapes. Drift guards: error if debugcheck loses anti-gaming or runtime-test loses Probe A/E. Lesson #88: any check visible to the thing it checks will be gamed unless it measures an external FACT.

- 2026-06-04: **v4.16.0** — REQ-1.19 GameReady fixed vs live doc (another 1.19 rejection). Our check/prose used a PROXY (ready() after fonts/UI) ≠ Yandex real rule (ready() EXACTLY when game interactive: menu available OR playable incl start animation; FAIL if green while progress-bar/black-screen showing, or seconds late, or red>90s). Likely genetic-lab cause: ready() in init() while still loading = "too early". FIXES: debugcheck v2.8→v2.9 (both copies identical) runtime 1.19.2 flags ready() TOO EARLY (before content) + TOO LATE (>1s after gameplay) + 90s window; yandex-sdk-integration rewrote #1 guidance to exact interactivity criterion; ADDED missing 1.19.1 loader check (debug-panel must show IT not IF; <script async src=/sdk.js onload=initSDK>); fix-moderation split 1.19 into .1/.2/.3. Honest limit: "interactive moment" judged by Yandex from loading video, our runtime approximates (content-paint/gameplay bracketing), instructions state exact rule. Lesson #87: a check on a PROXY ("after fonts") passes games that violate the REAL rule ("when interactive") — when moderation keeps rejecting a "covered" req, suspect a proxy and re-read the literal criterion.

- 2026-06-04: **v4.15.2** — Verified REQ-2.14 instructions vs live Yandex doc (user challenge). debugcheck CODE was already correct (langDetected<firstUserClick = before gameplay, tolerates loading interval). PROSE was off: corrected "ДО первого рендера"/"UI сразу" → "на старте, до игрового процесса" (Yandex explicitly allows brief loading text in another language before lang loads — our wording overstated/stricter-than-required); ADDED missing rule that SDK autodetect must be in ALL games even single-language/no-text ones. No code change (validator right), instruction accuracy only. Lesson #86: keep prose calibrated to the EXACT requirement not a stricter paraphrase — "before first render" sounds safer than "before gameplay" but invents work Yandex doesn't ask for and misrepresents the rule.

- 2026-06-04: **v4.15.1** — Fixed i18n-completeness.mjs REQ-8.2.3 false-positive at TEMPLATE source (was patched in project copy, reverted via sync). Bug: "hardcoded Russian in HTML" check decided runtime-managed via id/class/descendant only, ignoring data-i18n attr — but applyStaticLang() translates everything matched by querySelectorAll([data-i18n]). So <div class=x data-i18n=key>текст</div> → 8 false BLOCKERs (goal-stat-lbl, lab.dna_editor, points-label×2, etc). Fix: detect data-i18n(-*) attr, carry on chain+stack, covered-check returns true if el.dataI18n. Functional-tested (data-i18n passes, genuine hardcoded still flags). check-drift guard #9 prevents silent revert. Downstream AI dx was right: sync.bat xcopy /Y clobbers project fixes from template → fix must live in template (same as debugcheck/runtime-test divergence). Lesson #85: a false-positive blocker is as harmful as a missed bug — validator covered-logic must mirror what runtime ACTUALLY does, not a subset.

- 2026-06-04: **v4.15.0** — NEW /audit-requirements skill: checks Yandex release-notes + requirements page vs a baseline block (last audit date / page date / debugcheck version / known thresholds), reports "no audit needed" or the exact delta mapped to the Forge artifact to update. Audit-by-event not calendar; updates its own baseline after each full audit (invariant #17). CAUGHT while building it: Yandex tightened REQ-4.4 ad-gesture delay to a hard <=330ms (0.33s) limit (release-notes 14.08+25.09.2025); Forge used 500ms → an ad 400ms after click PASSED Forge but FAILS Yandex. Tightened all 6 thresholds (debugcheck interstitial+rewarded detect+check, runtime-test Probe A/B) 500→330ms; debugcheck v2.7→v2.8, both copies identical. Lesson #84: requirement THRESHOLDS drift silently ("follow a gesture"→"≤330ms" without renaming the rule); a check with the right rule but wrong number is a false pass — audit skill now tracks named thresholds.

- 2026-06-04: **v4.14.0** — Full Yandex requirements audit (live doc, last changed 5 May 2026) point-by-point vs Forge's ~85 debugcheck + 19 verifiers. Confirmed already-covered: 1.1/1.3/1.4/1.5/1.7/1.8/1.9/1.10.x/1.13.x/1.19.x/2.14/3.8/4.4/4.5/4.7/8.2.3 + SDK suite. ADDED to debugcheck v2.6→v2.7 (both copies identical): 1.6.1.7 WebGL-notice, 1.6.1.6/2.5 system-video-player, 1.6.1.2 keyboard-on-input, 1.18 URL-gating, 4.2 progress-before-ad. ADDED as MANUAL review items to fix-moderation map (can't static-check): 1.14 crash-on-rotate, 1.6.2.2 desktop aspect, 4.3 ad-orientation, 1.2.2 guest-play, 3.5 IP (usually store screenshots not code), +5.6/5.12. Documented out-of-scope (human/console): 2.x quality, 3.4 content, 5.x promo, 8.3 media. Lesson #83: audit value = honest 3-way split (covered / auto-checkable-so-add / inherently-manual-so-flag); faking a check for (c) is theater, omitting it lets it slip.

- 2026-06-02: **v4.13.1** — Killed the false-GREEN from missing puppeteer (live finding: "runtime-test puppeteer missing → silently skip → worthless GREEN"). runtime-test now AUTO-INSTALLS puppeteer when missing, then proceeds. If install fails → exit 3 = UNVERIFIED = BLOCKER (not skip). scripts/runtime-test.mjs previously did exit(0) "returning success" on missing puppeteer — a literal false GREEN — removed; both runtime-tests fail-loud now. release-ready: removed "(if puppeteer available)" escape hatch; exit-code contract (0=pass/2=usage/3=unverified=blocker). check-drift guard errors if either runtime-test regresses to silent-skip. Lesson #82: a silently-skipping test is worse than a failing one — missing tooling must auto-resolve or hard-block, never render as "passed".

- 2026-06-01: **v4.13.0** — ROOT-FIXED the "autotests pass but moderation fails" gap (user's genetic-lab got 6 Yandex rejections). Cause: BOTH validators diverged into strong+weak forks, weak ones active. debugcheck.js: yandex/templates=v2.6 (gestureDelta/AD_WITHOUT_GESTURE recency check) vs html5=stale v2.1 (only "ever clicked", no recency) — v2.1 passes ad-on-sim-auto-end (genetic-lab's bug). runtime-test.mjs: yandex/ has REQ-4.4 Probe A trap, scripts/ ~none. FIXES: consolidated debugcheck (v2.1→v2.6, byte-identical now); check-drift guard #7 (debugcheck must not diverge, ERROR); broadened Probe A blacklist (+showResult/onComplete/roundOver/simComplete/winGame — genetic-lab fired from showResult, old list missed it); check-drift guard #8 (runtime-test must keep 4.4 trap); release-ready hardened: "couldn't verify ≠ passed" (a 4.4/8.2.3 warn is an UNVERIFIED BLOCKER — the actual hole, since genetic-lab's ad only fires 3rd sim-end after 60s so short tests never triggered it). Lesson #81: when autotests "miss" things, often the good test exists but a STALE FORK runs — find which copy is active.

- 2026-05-31: **v4.12.1** — /asset-generation corrected against user's real tools.zip (304-file zarechye pipeline). REPLACED guessed extract_says.js with the REAL one (vm.runInContext + recursive command-tree walk {op:say,who,text,vtag}, md5 hash — my regex version would have extracted NOTHING from their format). ADDED missing voice post-process (run_voice_postprocess.bat: ffmpeg highpass→afftdn denoise→dynaudnorm, backup+skip) and voice audition (test voices before mass-gen, saves budget). Voice now documented as 3-stage: audition→generate→postprocess. voice_catalog example → real format (_model eleven_v3, vtag tone-override). FIXED latent bug in user's own script: Cyrillic inside if-errorlevel () block (Lesson #20) → English. Bundled reusable scripts, skipped add_red_symbol.py (zarechye one-off). Lesson #80: read the user's real code before trusting your reconstruction.

- 2026-05-31: **v4.12.0** — NEW /asset-generation skill: generate REAL external assets (not procedural) to raise audio-visual quality. Modeled on user's proven zarechye VN pipeline, generalized. Visuals via OpenRouter (Gemini image, multi-ref, rembg cutout, inherits /art-direction style); voice via ElevenLabs eleven_v3 (per-role voices + audio-tags, says.json→mp3); SFX via ElevenLabs Sound Generation; music = Suno 5.5/4.5 prompt sheets (no API — honest manual step). Core deliverable: EMITS a filled-in generate_all.bat from asset-catalog.json (user double-clicks, no hand-edit), respects .bat rules + checked by check-bat-comments/encoding. Bundled reference scripts (generate_image/sfx/voice.py made Forge-portable via key-file ROOT search; NEW extract_says.js the user's bat referenced but never provided; example catalogs + Suno template). Keys from .openrouter_key/.elevenlabs_key files (never hardcoded); Forge EMITS, user RUNS (paid APIs never called by Forge, budget=Tier-3 stop, skip-existing default). 114 skills. Lesson #79: generalize the user's working pipeline, don't invent a parallel one.

- 2026-05-31: **v4.11.7** — Pre-handoff bug audit (user taking the project). Method: 19 verifiers + syntax-check all 37 .mjs + validate all JSON + hook-script existence + frontmatter + manifest↔fs + xref resolution. FIXED 2 real bugs: fix-moderation referenced non-existent /verify-yandex (→ /release-ready yandex) and /build-release (→ build-yandex-3zips.mjs / auto-release) — dead commands mid-moderation-fix, caught by check-drift. VERIFIED CLEAN: all scripts parse, all JSON valid, both hook configs resolve, 113 skills valid frontmatter (name=2 flags = false positives in skill-creator template examples), 178 manifest entries all resolve (no stale), 4 verifier exit-2 = correct N/A guards not bugs, remaining /frontend-design warning = legit Anthropic external skill. Lesson #78: audit job is finding the 2 actually-broken among 200 fine, without crying wolf on false positives.

- 2026-05-30: **v4.11.6** — Monetization policy RESOLVED: ads = Priority 1 (default), IAP = Priority 2 (opt-in, only when user explicitly asks) — tiered model, not a ban. /monetization-design restructured: decision gate at top (ask "IAP?" if unsure), Tier 1 = ads + earned-only currency (added leaderboard source), Tier 2 = IAP catalog/funnel/paywalls all marked "only if requested". Recorded as architectural invariant #18 in CLAUDE.md (survives new sessions — the v4.11.4 lesson). Other IAP-referencing skills verified to only mention/audit, not default-push. BONUS: caught+fixed rotate-changelog.mjs stale hardcoded KEEP list that rotated out NEWEST changelogs (no data lost — byte-verify; restored inline); KEEP now computed by semver (invariant #17), regression-tested. Lessons #76 (default+opt-in beats ban), #77 (a "keep latest 3" tool must derive "latest").

- 2026-05-30: **v4.11.5** — Tier 3 (diagnostic): all 3 "bigger bets" were misdiagnoses or intentional design — NET CODE CHANGE ZERO. (1) "collapse dual-install": .bat/.ps1/.sh is cross-platform by design not dup; plugin-path is opt-in+untested, no hook double-fire for manual usage. (2) "build zip from MANIFEST": would BREAK releases — 4 files intentionally excluded (baseline + MANIFEST.txt are user-mutable state upgrade.ps1 must not delete); tree ships zero junk so problem doesn't exist; MANIFEST is "what-upgrade-may-delete" not "zip-contents". (3) portability: strategic future choice, not a bug. SHIPPED: README plugin-install "beta"→"deferred—not active" (was misleading; needs absent GitHub remote). Lesson #75: bigger bets deserve harder diagnosis not bolder action — a tier whose honest output is "don't change this, here's why" is a success.

- 2026-05-29: **v4.11.4** — Tier 2 (diagnostic-first). 3 of 4 candidate fixes were WRONG: runtime-test files not dupes (2 distinct tools, rename≠merge), UI-cluster not redundant (layers; ui-pipeline orchestrates), all 19 verifiers earn keep (infra-called, not ceremony). ONE real gap: "technique without a target" — producer skills with criteria but no OUTPUT self-eval step. Added subject-specific self-check to 5: game-design (play 3min rubric), level-design (prove solvability + walk curve), shader-fx (render≠compile), 3d-perf (measure-after, before→after numbers), deepen-game (deeper vs bigger). HONEST STOP on 6th (monetization-design): ads-only/no-IAP lives in CHAT context not repo (IAP is built-in across 9 skills + subscription-design); partial edit made skill self-contradictory → reverted whole thing, surfaced as user decision. Lesson #74: edit conflicting with skill body is worse than no edit; chat-policy ≠ repo-policy.

- 2026-05-29: **v4.11.3** — Tier 1 of 3 planned maintenance tiers (self-audit, turning rigor inward). FIXED real bug caught live: update-advisor-catalog.mjs digit-blind regex (/[a-z]/ rejected /3d-perf) made it disagree with check-cross-refs (112 vs 113, false "missing: 1"); fixed both to [a-z0-9] → now agree, Missing: 0. NEW check-drift.mjs (19th verifier, MCP auto-discovered): orphan dirs, CLAUDE.md size, advisor↔fs agreement, MANIFEST↔fs, broken /skill xrefs, version consistency — fail-soft, found 3 genuine minor xref warns. bump-version.mjs: proactive CLAUDE.md size guard + updated next-steps (rotate + check-drift before ship); did NOT auto-mutate CLAUDE.md (data-loss history). Lesson #73: Forge under-applied its own discipline to itself; a self-audit verifier is cheap insurance against accretion.

- 2026-05-29: **v4.11.2** — Full freshness/compatibility audit ("проверь всё") → Opus 4.8 / Claude Code currency. Verified current: model IDs, API surface, anthropic-version 2023-06-01, hooks format, plugin/marketplace schema, agents dir, frontmatter (113 valid), Three.js 0.184 — nothing critically broken. APPLIED 8: /effort xhigh in mvp-to-yandex + auto-release prereqs (Opus 4.8 defaults high); effort API param in ai-integration; dynamic workflows documented in advisor as OPTION (not /goal replacement); CC "latest" → v2.1.153 noted (floors kept); /reload-skills + live change-detection in GUIDE; MCP SDK ^1.0.0→^1.29.0; simplex-noise 4.0.1→4.0.3; vk-bridge @latest→@3.0.2. HONESTLY DROPPED 2: disallowed-tools enforcement (CC bugs #37683/#18837/#14956 — "parsed but not enforced"; kept workspace-discipline hook which actually works); npm-install deprecation note (N/A — project never npm-installs CC). Lesson #72: "use latest" ≠ "adopt every flag" — verify enforcement before relying; a hook that works beats a frontmatter field that doesn't.
- 2026-05-29: **v4.11.1** — NEW `/do` command (commands/do.md): "advisor that ACTS". User works mostly via advisor, but advisor only formulates a prompt to copy-paste back. /do reads project context (same wiki files), routes via the advisor catalog, and EXECUTES the skill on the real files + keeps working — no prompt handoff. Universal (games/apps/Forge-tooling). Visual requests auto-route through /art-direction (spec → build → self-critique before showing user). "Переделай" = redesign to spec, not a tweak. Keeps working via Tier 1/2/3 policy; escalates large measurable outcomes to /goal. Wired: advisor top-pointer ("хочешь чтобы СДЕЛАЛ → /do"), Quick-Start table lists /do first, README leads with /do, cross-refs whitelist += 'do'. Commands 3 → 4.
- 2026-05-29: **v4.11.0** — Visual quality overhaul (fixes #1 user pain: 3D/pixel/UI come out as programmer-art, need many "сделай нормально" rounds). Root cause: generate-first with no defined target + user-as-quality-loop. NEW /art-direction (architectural keystone): per-game spec (exact hex ramps, detail-density target killing "просто стены", hero details) + self-critique loop (6-axis rubric on rendered output, revise BEFORE showing user → pass-3 quality on pass 1) + redesign-vs-patch rule. NEW /pixel-art (tactical, fills real gap — no 2D sprite skill existed): palette ramps, silhouette-first, animation frame budgets w/ anticipation+smear, sprite-sheet + SpriteAnim helper, code-drawn (zero downloads). WIRED into /three-setup (new "Step 6 — never deliver a bare scene": furnish to spec on pass 1 — the 3D "просто стены" fix), /visual-upgrade (Phase -1 + sprites route to /pixel-art), /visual-style, /procedural-geo. UI fix /fix-ui: patch-vs-redesign triage ("переделай" → real redesign via /art-direction, not a tweak) + genre→design/UX table. Registered /art-direction, /pixel-art AND latently-missing /3d-perf in advisor (112/112, no drift; 113 skills). Lesson #71: visual quality = spec+critique-loop problem, not technique — skills had techniques, lacked a target + internal evaluation pass.
- 2026-05-29: **v4.10.38** — Full audit + Opus 4.8 modernization + a11y/D1 strengthening. AUDIT: removed brace-expansion artifact dir `.claude/skills/{info-hierarchy,layout-system,ui-review}/` (empty, not in manifest); trimmed CLAUDE.md 91 KB → 28.9 KB via new `scripts/rotate-changelog.mjs` (section-boundary parse + byte-accounting verify before delete — guards v4.10.23-style data loss; honors "keep latest 3" footer rule). OPUS 4.8 (released 2026-05-28, ID claude-opus-4-8): fixed legacy `claude-sonnet-4-20250514` → `claude-sonnet-4-6` in pwa AI references; added current model catalog (haiku-4-5/sonnet-4-6/opus-4-8) + acceptance criterion to stack/ai-integration; documented adaptive-thinking-only API surface (budget_tokens + sampling params removed in 4.7+) — Forge generates zero deprecated patterns (audited clean). GRAPHICS: Three.js 0.171→0.184 across visual-style/three-setup; new accessibility section in /visual-style (no hue-only state encoding, Okabe–Ito CVD-safe palette, WCAG AA contrast). GAMEDESIGN: new "First Session — 60 seconds" Step 2.5 in /game-design (biggest D1 lever: 60s contract, teach-by-doing onboarding, forgiving first run, up-note ending). TOOLING: fixed `bump-version.mjs` GUIDE.md/dashboard.html rules (were stale, silently no-op'd — manual sed no longer needed). KNOWN: two runtime-test.mjs remain specialized (variant/screenshot vs debugcheck-injection), consolidation deferred until a real game build exists to validate merged gate.
- 2026-05-27: **v4.10.37** — FIX Forge tooling: build-yandex-3zips.mjs искал support files (debugcheck.js/cheats-base.js/screenshots.js) в platforms/yandex/tools/ — wrong subfolder, они в platforms/yandex/templates/. tools/ содержит только game-screenshot-ext/. Fixed SUPPORT_DIRS к [templates/, templates/html5/]. Plus runtime-test.mjs scenarioAssets теперь whitelist'ит debugcheck.js/cheats-base.js/screenshots.js как Forge dev-tooling (не game assets) — раньше runtime-test fail'ил если игра ссылалась на debugcheck.js но файл не bundled. Plus build-yandex-3zips теперь удаляет <script src> теги для forbidden files из index.html (не только сами файлы) — production build больше не имеет stale debugcheck.js reference. Lesson #70: tooling path bugs masquerade as game bugs — separate Forge-infra failures from product failures.
- 2026-05-21: **v4.10.36** — 5 Three.js 3D graphics skills added. /three-setup (scene boilerplate: WebGPU+WebGL2 fallback renderer, camera, lighting rig, Three.js bundled LOCALLY not CDN). /visual-style (16 looks via post-processing: toon/low-poly/neon/voxel/hologram/matcap/etc, EffectComposer pipeline). /procedural-geo (geometry by code: simplex noise terrain, low-poly clouds, voxel, hex grid, seeded RNG — zero asset downloads, tiny zip). /3d-perf (draw calls <100, InstancedMesh, geometry merging, LOD, Draco/KTX2 compression, memory disposal). /shader-fx (custom GLSL/TSL: rim light, dissolve, water, hologram, force field, toon, vertex animation). All emphasize local bundling (Yandex CDN compliance). /game router knows 3D skill chain. Fixed check-cross-refs regex — skill names starting с digit (3d-perf) were missed. Lesson #69: code-generated graphics sidestep asset-size + CDN constraints simultaneously.
- 2026-05-20: **v4.10.35** — FIX sync.bat broke с 'Непредвиденное появление: or'. Root cause: 23 `::` style comments inside () blocks (for/if bodies). cmd.exe `::` is label-comment — works at top-level но breaks parser inside parenthesized blocks, especially когда comment contains slashes (/release-ready, /fill-yandex) или apostrophes. REM comments work everywhere. Converted all 23 indented :: к REM. New check-bat-comments.mjs validator (18 verifiers total) catches :: inside () blocks. Lesson #68: cmd.exe :: vs REM — :: only safe at top-level, REM safe everywhere; inside blocks always REM.
- 2026-05-20: **v4.10.34** — External CDN refs are Yandex release blocker. New check-external-cdn.mjs scans HTML/JS для <script src=https://...>, ES import 'https://...', <link href=https://...>, @import url(https://...), new Worker('https://...'). Whitelists Yandex SDK domains (yandex.ru/net/com, yastatic.net etc). 17 known CDNs flagged CRITICAL (cdnjs/jsdelivr/unpkg/skypack/esm.sh/Google Fonts/Tailwind CDN/etc). New /bundle-libs skill auto-downloads CDN libs к assets/lib/, replaces refs с relative paths. Integrated в /release-ready yandex как MANDATORY gate, в /mvp-to-yandex Phase 4 before runtime-test, /goal conditions 12→13. Lesson #67: sandbox compliance constraints are silent blockers — game runs perfectly in dev, fails moderation. Need explicit validator per sandbox rule.
- 2026-05-19: **v4.10.33** — upgrade.bat + setup.bat wrappers fix chicken-egg Mark-of-the-Web problem. Pre-v4.10.33: extracted upgrade.ps1 was blocked by MotW, couldn't unblock other files because itself blocked. Now: .bat files don't get MotW after ZIP extract, launch .ps1 via -ExecutionPolicy Bypass. Double-click upgrade.bat = one-click upgrade без 'not digitally signed' errors. README + GUIDE recommend .bat as primary path. setup.ps1 also adds Unblock-File first step (was missing). All .bat files ASCII-only (Lesson #20 enforcement через check-bat-encoding). Lesson #66: chicken-egg bootstrap problems require alternative trust path — when fix-script itself needs unblock, ship alternative loader that doesn't need it.
- 2026-05-15: **v4.10.32** — Fix /mvp-to-yandex 3 gaps: (1) собирает только production zip — Forge standard requires 3 (production + debug + marketing). New build-yandex-3zips.mjs creates все 3 variants с правильными support files (debugcheck.js/cheats-base.js/screenshots.js). (2) runtime-test проверяется against production — слабее чем debug-build с behavioral probes. Updated default к --variant=debug. (3) UI/UX review skipped — added /ui-review --auto step после Phase 4 build с zero CRITICAL gate. Goal conditions расширены с 9 к 12 (3 zips exist, UI/UX zero CRITICAL, runtime against debug). build-yandex-3zips added к sync. runtime-test gets --variant flag. Lesson #65: build matrices не just deliverable — they're testing surfaces, validators must run against richest variant.
- 2026-05-15: **v4.10.31** — /mvp-to-yandex autonomous workflow для small MVP projects. One command: analyze → 7-day retention plan (genre-aware) → implement features+content+balance+monetization (aggressive ad: rewarded х2/boosters/hard currency, no IAP) → build production yandex zip → localize RU+EN+TR+10 stub aliases → generate all documents + art prompts → release-ready GREEN. Uses /goal v2.1.139+. Tier 1 auto-decides (features, balance, content quantity), Tier 2 quick asks (genre ambiguity, art style), Tier 3 critical blocks (missing creds, custom code conflicts, >4hr estimate). Routers /game auto-detect 'mvp'/'доведи до релиза'/'прогон' keywords. Also fixed bug в update-advisor-catalog.mjs где script overwrote existing Recently installed section вместо append к ней. Lesson #64: autonomous workflows для small MVPs scale через genre-aware decision matrices.
- 2026-05-14: **v4.10.30** — Restore keywords field как Yandex Console field (v4.10.21-v4.10.29 incorrectly said field doesn't exist). Keywords back to required для Yandex. fill-yandex SETUP_GUIDE §3 adds keywords section (per-language joined strings). §7 Лидерборды теперь имеет multi-language display names table (RU/EN/TR/...). check-setup-guide.mjs new checks: wrong_keywords_claim (catches old skill output claiming field doesn't exist), leaderboard_no_multilang (table without per-lang columns). yandex-fields-constraints.md adds 'Verification status per constraint' table — explicit honesty о том что VERIFIED vs ASSUMED. Lesson #63: ship verification status, not certainty.
- 2026-05-14: **v4.10.29** — AppMetrica analytics integration для RuStore. New /appmetrica-integration skill auto-detects wrapper type (TWA/Capacitor/Cordova/Native), adds mobmetricalib dependency + manifest meta-data + activation code + JS bridge для WebView. New check-appmetrica.mjs validator (16 verifiers total) catches missing dep, placeholder API key, missing permissions, missing activate() call. /release-ready rustore теперь имеет soft mode (--soft, documents+assets only) и hard mode (default if wrapper present, includes AppMetrica + APK build verification). reference/rustore-requirements.md shipped — SDK requirements, AndroidManifest entries, moderation rules. Lesson #62: analytics is de-facto release gate, not optional infra.
- 2026-05-14: **v4.10.28** — /ui-review переписан screenshot-driven (v4.10.27-: principles-based, generic 'improve hierarchy' advice). New mandatory inputs: screenshot attached, path к existing, --auto flag (auto-snapshot via runtime-test.mjs), или HTML file path. 5-phase workflow: zone enumeration → 8-point checklist per zone → source file mapping → structured violations table → STOP (no auto-fix). Each violation needs coordinates OR pixel measurement OR specific element name. Severity ranking (CRITICAL/MAJOR/MINOR/NIT). Anti-patterns section rejects generic advice. runtime-test.mjs gets --screenshot=true flag + --viewport= для capture. Legacy principles-based mode kept в footer для wireframe stage когда UI ещё не существует. Lesson #61: vision-based skill > principles-based для existing UI — generic checklists переписывают specific observations.
- 2026-05-14: **v4.10.27** — pre-claim-fixed Stop hook. Detects when AI claims completion ('fixed', 'готово', 'исправил', 'done', 'applied') в final message but git diff is empty — common AI hallucination failure mode where AI generates 'I fixed X' text без actually calling Edit tool. Hook blocks Stop and injects reminder: 'run git status, if no changes call Edit tools really, then verify with git diff'. Multilingual phrase list (RU+EN). Historical context detection (skips 'fixed yesterday'). Bypass: FORGE_SKIP_FIXED_CHECK=1. Fail-open для non-git folders. Wired в settings.json и plugin-hooks.json after stop-flush. 9 hooks total. Lesson #60: behavioral gates prevent AI hallucinations through verification, not just trusting AI's self-report.
- 2026-05-14: **v4.10.26** — FIX: real Yandex field constraints. v4.10.20-v4.10.25 schema lied (subtitle 3-60 vs real 20-70, seo 80-200 vs 50-160, about 300-2000 vs 300-1000, how_to_play 100-1500 vs 300-1000). Plus new: NO CAPS rule в title/subtitle (no all-caps words >=4 chars, supports Cyrillic/Latin/Turkish с diacritics). Created reference/yandex-fields-constraints.md (shipped reference). check-store-listing.mjs CAPS regex fixed for Cyrillic (\b doesn't work, use whitespace split). Updated fill-yandex Step 9 example: 'Бабкин цех' not 'БАБКИН ЦЕХ', emoji removed from how_to_play, hook style 'Идл-кликер про...' not 'Тапай...'. ASO principles: hook says WHAT not WHAT TO DO, primary keyword density 2-3%, CTA в seo first 100 chars. Tested на user's 3 broken Самогонщик files — catches all 10 violations correctly. Lesson #59: schema is contract with reality, validate against real Console snapshots not assumed constraints.
- 2026-05-14: **v4.10.25** — /auto-release skill (uses Claude Code v2.1.139+ /goal command для hands-free release loop). Sets goal 'release-ready GREEN for {platform}', Claude iterates fix→validate→fix пока зелёное. /game и /app routers auto-detect 'до зелёного' / 'автономный релиз' phrases. Advisor section про /goal use cases + anti-patterns. README CC version bumped к v2.1.139+. Lesson #58: outcome-driven workflows через /goal — completion condition должен быть machine-checkable (exit codes, file presence), не subjective.
- 2026-05-13: **v4.10.24** — sync propagates verifier scripts + schemas/ to siblings. Before: validators only lived в Forge folder, sibling /release-ready couldn't run check-store-listing.mjs, runtime-test.mjs, check-setup-guide.mjs (skill referenced them but file not present). Now: 22 verifier/helper scripts copied к each sibling on sync + schemas/ folder mirrored. Lesson #57: skill references к scripts/ require those scripts physically present в caller's working dir.
- 2026-05-13: **v4.10.23** — Cleanup commit. Restored 3 corrupted CLAUDE.md version headers (sed had renamed v4.10.5/6/10 → v4.10.22). Rotated v4.10.0-v4.10.4 changelogs к docs/CHANGELOG.md. Reconstructed v4.10.7-v4.10.22 changelog history с Lessons #42-55 full context. Deduped wiki/_map.md (47 unique versions). Created scripts/bump-version.mjs — safe version bump that does NOT sed historic changelogs. Updated README с /find-skill, GUIDE с upgrade.ps1. Promoted Lessons #44, #46, #51 к Architectural Invariants #15-17. Lesson #56: version bumping must preserve historic changelog headers.
- 2026-05-13: **v4.10.22** — SETUP_GUIDE comprehensive template + validator. Step 5 в fill-yandex переписан с 5 секций → 17 секций matching user's real Самогонщик SETUP_GUIDE.md. New check-setup-guide.mjs validator (15 verifiers total): 17 sections, no placeholders, no invalid tags/categories в positive context, store-listing consistency, reference links. Anti-pattern context detection (isAntiPatternLine — ❌/НЕ ставь lines skipped). Integrated к release-ready as Yandex mandatory gate. Lesson #55: output templates must match real-world output structure, not derive simplified versions.
- 2026-05-13: **v4.10.21** — FIX v4.10.20 wrong schema. category is array (1-3 items) per real Yandex Console behavior. Shipped Yandex reference data: 25 valid categories + 700 valid tags. fill-yandex Step 2 rewritten — MANDATORY read reference files. Removed old wrong category list ('Аркады', 'Бродилки', 'Клик-тап' don't exist). Tags AI mistakes table added (idle → тапалки, СНГ → на русском+мемы). Lesson #54: ship reference data inside skill for closed-vocabulary systems.
- 2026-05-13: **v4.10.20** — Store-listing schema gate. schemas/store-listing.schema.json + check-store-listing.mjs validator. fill-yandex Step 9 rewritten как strict JSON (was .md). Added 8 FORBIDDEN fields list (catches AI invention: _comment, _removed_fields, developer_comment, ageRating, etc). ASO/SEO writing principles section. Validator integrated к release-ready как mandatory gate. Tested on user's actual broken file — catches all 9 violations. 14 verifiers total. Lesson #53: schema validation gates AI invention.
- 2026-05-13: **v4.10.19** — Advisor catalog updated. Added Quick Start section (/game, /app, /continue at top). Added Infrastructure awareness section (runtime-test, upgrade.ps1, MANIFEST.txt, check-* scripts). Reframed legacy commands as 'specific skills, routers prefer'. Lesson #52: advisor needs manual review each major infrastructure change.
- 2026-05-13: **v4.10.18** — MANIFEST.txt automatic orphan detection. generate-manifest.mjs + apply-manifest.mjs scripts. upgrade.ps1/upgrade.sh now use manifest for catch-all cleanup — no more hand-maintained $Orphans lists missing entries. Lesson #51: hand-maintained lists are process debt.
- 2026-05-13: **v4.10.17** — runtime-test.mjs (5 scenarios: startup/lang/assets/dom/sdk via headless Chrome) integrated as MANDATORY gate in release-ready. art-prompts upgraded with store compliance (Yandex 5.6/8.3.5, RuStore 6.4, iOS HIG, Android adaptive) + 3-test verification (squint/distance/mask) + 7 anti-patterns. Lesson #50: AI-admitted gap = automated fix needed.
- 2026-05-13: **v4.10.16** — upgrade.ps1 encoding fix (em-dashes + cyrillic without BOM crashed Windows PowerShell 5.x). Rewrote ASCII-only with UTF-8 BOM. New verifier check-ps1-encoding.mjs (13 verifiers total). Lesson #49: Windows encoding is permanent landmine - enforce ASCII or BOM for all .ps1/.bat files.
- 2026-05-13: **v4.10.15** — upgrade.ps1 + upgrade.sh. Copy-with-replace + one script does everything: unblock MoW, remove orphans (8 files since v4.10.9), fix nested dupes, sync advisor. No more Remove-Item dance. Lesson #48: migration tooling > documentation.
- 2026-05-08: **v4.10.14** — /find-skill discovery workflow (Vercel Labs inspired, dual-tier). Internal search engine + marketplace integration via npx skills. Forge adaptation wrapper для public skills. Auto-update advisor catalog. /game and /app routers fall back к /find-skill для unclear capability requests. Lesson #47: discoverability is its own skill.
- 2026-05-08: **v4.10.13** — Full audit (19 surface points). Hardened Copy-IfNeeded helper к use robocopy для -Recurse. Created check-nested-dirs.mjs detector + --fix mode. 12 verifiers total. Lesson #46 updated: Robocopy/rsync only trustworthy для idempotent dir merge.
- 2026-05-08: **v4.10.12** — sync.ps1 platforms\ nesting bug fixed. After multi-sync, siblings accumulated platforms/platforms/ nested dirs. Replaced Copy-Item -Recurse -Force с robocopy /E (or /MIR strict). Same class as v4.10.3 fix для skills. Lesson #46: audit все Copy-Item -Recurse calls.
- 2026-05-03: **v4.10.11** — Auto-cleanup orphan wrappers на setup. Fixes user pain про copy-with-replace zip extract. setup.ps1/setup.sh runs cleanup-orphan-wrappers.mjs automatically. sync.ps1 enhanced verification для EXTRA files. Lesson #46: fix tool when user does common case you didn't expect.
- 2026-05-03: **v4.10.10** — Audit + fixes. Found Claude Code v2.1.101+ auto-merge skills→commands (validates v4.10.9 simplification). Fixed: session-start.mjs hook message, README workflow examples, GUIDE main scenarios, dashboard.html prominent quick-start. Bumped min Claude Code version к v2.1.101+. Lesson #45: engine capabilities can obsolete codebase patterns.
- 2026-05-03: **v4.10.9** — Radical simplification: 9 commands → 3 (/game, /app, /continue). Smart routers с decision tables — auto-detect intent from context. 102 skills unchanged, just less top-level surface. Lesson #44: user cognitive load is the bottleneck.
- 2026-05-03: **v4.10.8** — Mandatory auto-categorization в /ui-pipeline + /info-hierarchy. Skill physically не может работать generic — blocks без category determination + explicit pattern citation. Closes loop для apps + games equally. Lesson #43: reference materials need enforcement gates.
- 2026-05-03: **v4.10.7** — Deep research yields genre/category-aware UX patterns base. patterns/games.md (7 genres, 342 lines) + patterns/apps.md (10 categories, 415 lines). All UX skills (info-hierarchy, layout-system, ui-review, ui-pipeline) reference these. Lesson #42: deep research yields specialized skills, generic doesn't.
- 2026-05-03: **v4.10.6** — /ui-pipeline orchestrator (5 steps: audit -> hierarchy -> layout -> implement -> verify). Single command вместо ручного вызова 3 skills по очереди. Lesson #41: orchestrator before specialists.
- 2026-05-03: **v4.10.5** — UX/UI Systems Designer trio: /info-hierarchy + /layout-system + /ui-review. Integrated в /design-pipeline (8 specialists для games, 7 universal для apps). Lesson #40: visual ≠ UX/UI design.
- 2026-05-03: **v4.10.4** — UX fix: questions visible, hooks terse (1-line), Architectural Invariant #14 "wiki cleanup before turn end". Stop hook output 6 lines -> 1. Lesson #39: hook timing matters as much as hook content.
- 2026-05-01: **v4.10.3** — sync.ps1 silent failure fix. Copy-Item -Recurse → robocopy для reliability. Post-sync verification (count check + warn if siblings have less skills). Lesson #38: trust verification, не trust reporting.
- 2026-05-01: **v4.10.2** — wiki-audit robustness fix. Frontmatter+entry parsing, 5-min grace, 10s tolerance, FORGE_WIKI_AUDIT_VERBOSE=1 debug. Diagnosed from real genetic-lab session. Lesson #37: tolerances are calibration params.
- 2026-04-29: **v4.10.1** — slash command wrappers (.claude/commands/). /pipeline /start /continue теперь принимают args как настоящие slash commands. Lesson #36 — verify mechanism before extending.
- 2026-04-29: **v4.10.0** — Step 0 Discovery в /pipeline. Content-based document classification (не по filename). Skip Step 3 если design покрыт docs. Lesson #35 — patterns extracted from concrete usage.
- 2026-04-29: **v4.9.6** — dashboard path bugs: literal \n in prompt, PROJECTS_ROOT auto-heal, separate projects root setting (button "📁 Папка проектов").
- 2026-04-29: **v4.9.5** — dashboard: status frozen, multi-select status filters, description toggle. Schema migration v4 → v5 fixes broken legacy mappings.
- 2026-04-29: **v4.9.4** — project name в statusline (Strategy Runner · [Q1-004] ...). Detection: wiki/_map.md → package.json → folder basename.
- 2026-04-29: **v4.9.3** — wiki-audit infinite loop fix. Use session START (parsed from log content) instead of log mtime. Lesson #34: audit triggers must use immutable references.
- 2026-04-29: **v4.9.2** — sync.bat false-success bug + verifier comment-skip bug. Lesson #32 (eat own dog food) + #33 (verifiers need adversarial testing).
- 2026-04-28: **v4.9.1** — hotfix: dashboard prompt paths (mixed slashes, broken cd; chain). Real-world bug from user session.
- 2026-04-28: **v4.9.0** — 8-iteration release: lesson rotation policy + skill categorization (`kind:` frontmatter) + /start auto-invocation chain + check-pipeline-state verifier + localStorage migrations + dashboard visual regression (structural diff) + validator regex generalization (`detectImportedNames`) + Forge MCP server
- 2026-04-28: **v4.8.0** — drift prevention (3 verifiers + tolerance fix) + Architectural Invariants section (12 permanent rules) + App Track complete (education + social foundations)
- 2026-04-27: **v4.7.10** — App Track Iteration 2: per-category foundations (health/finance/business/saas) + check-no-float-money verifier
- 2026-04-27: **v4.7.9** — App Track Iteration 1: universal app foundations (data-model, onboarding, search, permissions, subscription) + classification + product-metrics app-aware + design-pipeline mode-aware
- 2026-04-27: **v4.7.8** — Full pipeline orchestration: /pipeline + /product-metrics + /design-pipeline + /autopilot + credentials all 9 platforms
- 2026-04-27: **v4.7.7** — workspace-discipline hook + skill text + verifier
- 2026-04-26: **v4.7.6** — i18n foundation as default (ru+en runtime)
- 2026-04-26: **v4.7.5** — advisor становится context-aware
- 2026-04-25: **v4.7.4** — edit button restored (z-index fix)
- 2026-04-25: **v4.7.3** — dashboard preview images + sort
- 2026-04-25: **v4.7.2** — dashboard path sanitization
- 2026-04-24: **v4.7.1** — sync.bat ASCII fix (cmd.exe parser bug)
- 2026-04-24: **v4.7.0** — Steam + VK Play platforms (3 → 9 platforms)
- 2026-04-23: **v4.6.4** — advisor catalog updated to v4.6
- 2026-04-22: **v4.6.3** — dashboard accuracy + /start research integration
- 2026-04-22: **v4.6.2** — UTF-8 BOM fix for PowerShell 5.1
- 2026-04-22: **v4.6.1** — path-flexibility fix
- 2026-04-21: **v4.6.0** — auto-research конкурентов в /start phase 0
- ранее: v4.5.x (sync mode safe merge), v4.4.x (research-references), etc.

### In Progress

Idle — awaiting user requests for v4.8 backlog or hot fixes.

### Next (v4.8 backlog — priority order)

1. **Forge wiki/ self-application** — applies own pattern (wiki/, decisions/, plan/) to itself. PARTIALLY DONE: this file is the start.

2. **Architectural Invariants section в CLAUDE.md** — distilled wisdom from lesson #1-27 в compressed form. Not changelog, but rules. To be at top of CLAUDE.md so Claude sees on every session.

3. **`scripts/check-bat-encoding.mjs`** — verifier для .bat файлов: scan non-ASCII inside `()` blocks (causes cmd.exe parser crash, see lesson #20).

4. **`scripts/check-cross-refs.mjs`** — automated audit advisor catalog vs filesystem (avoids drift like in v4.7.5 where 4 skills disappeared from catalog after rewrite).

5. **Skill categorization tactical/architectural** — review всех 81 skills, mark which are "auto-invoked from /start" (architectural) vs "called when needed" (tactical). I18N-foundation = architectural, /localize = tactical.

6. **Architectural skills auto-invocation** — `/start` should auto-call architectural skills (i18n-foundation, possibly auth-sync, error-boundary, save-system) without user opt-in. Current state: only i18n-foundation has this.

7. **wiki-audit hook tolerance** — currently strict mtime comparison gives 1-second false positives. Add ±2s tolerance.

8. **Visual regression for dashboard** — headless screenshot + diff before package, catches z-index/layout regressions like v4.7.4.

9. **Steam validator: aliased ESM imports** — already handled in v4.7.7 audit but pattern needs to apply to other platforms.

10. **MCP integration** — Forge as MCP server providing skills/agents to other Claude instances.

## Quick Links

### Architecture
- [[architecture/stack]] — full tech stack table
- [[architecture/data-flow]] — how Forge processes a project from sources to release

### Key decisions (last 12)
- [[decisions/001-three-folder-discipline]] — GameIntegration / WorkProgress / Release separation
- [[decisions/002-knowledge-base-vs-skills]] — `.claude/skills/` (commands) vs `./skills/` (KB)
- [[decisions/003-template-by-path-equality]] — sync detects template by absolute path, not folder name
- [[decisions/004-encoding-rules]] — .bat ASCII-only inside `()`, .ps1 BOM for PS 5.1, .sh LF
- [[decisions/005-auto-research-phase0]] — /start mandatory Phase 0a research-references
- [[decisions/006-platform-completeness-check]] — 18 integration points × N platforms automated audit
- [[decisions/007-i18n-runtime-default]] — ru+en runtime, NOT compile-time, NOT 13 languages upfront
- [[decisions/008-context-aware-advisor]] — advisor reads wiki/ ДО formulation
- [[decisions/009-workspace-discipline-three-layers]] — skill text + hook + verifier для each rule
- [[decisions/010-architectural-vs-tactical-skills]] — auto-invoke architectural, opt-in tactical
- [[decisions/011-wiki-audit-mtime-tolerance]] — ±2s tolerance для filesystem timestamp comparison (v4.8.0)
- [[decisions/012-lesson-rotation-policy]] — 3-tier classification: principle / pattern / incident (v4.9.0)

### Plans
- [[plan/v4.8-roadmap]] — backlog details

### Memory
- [[_current]] — active session state
- [[changelog]] — high-level version diary
- [[tech-debt]] — known TODOs not yet prioritized
