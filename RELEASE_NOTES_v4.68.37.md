# Project Forge v4.68.37

## Mixed TBD assertion guard

Phase 1 research evidence now rejects an uncited positive external assertion (`verified`,
`confirmed`, `requires` and Russian equivalents) even when the same line contains `TBD` or another
unknown-value label. This prevents a model from hiding a factual claim inside a nominally
unverified metric or platform field.
