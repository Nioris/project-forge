# WorkProgress/ — рабочие копии

Сюда копируются проекты из `GameIntegration/` и здесь происходит вся работа:
правки кода, интеграции SDK, локализация.

НЕ трогай руками — управляется слэш-командами (/release ...).

Каждый проект содержит `PIPELINE.md` — трекер этапов.
