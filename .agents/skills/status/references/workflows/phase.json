{
  "schemaVersion": 1,
  "id": "phase",
  "mode": "phase",
  "start": "execute",
  "nodes": {
    "execute": { "type": "agent", "transitions": { "in_progress": "execute", "completed": "done", "user_decision_required": "wait-user", "retryable_failure": "repair", "provider_failure": "retry-provider", "environment_failure": "blocked", "blocked": "blocked" } },
    "repair": { "type": "agent", "maxAttempts": 3, "exhaustedTo": "blocked", "transitions": { "in_progress": "repair", "completed": "done", "user_decision_required": "wait-user", "retryable_failure": "repair", "provider_failure": "retry-provider", "environment_failure": "blocked", "blocked": "blocked" } },
    "retry-provider": { "type": "system", "maxAttempts": 3, "exhaustedTo": "blocked", "transitions": { "in_progress": "execute", "completed": "done", "provider_failure": "retry-provider", "environment_failure": "blocked", "blocked": "blocked" } },
    "wait-user": { "type": "decision", "transitions": { "in_progress": "execute", "completed": "execute", "user_decision_required": "wait-user", "blocked": "blocked" } },
    "blocked": { "type": "terminal", "transitions": {} },
    "done": { "type": "terminal", "transitions": {} }
  }
}
