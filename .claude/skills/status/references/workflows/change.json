{
  "schemaVersion": 1,
  "id": "change",
  "mode": "change",
  "start": "implement",
  "nodes": {
    "implement": { "type": "agent", "transitions": { "in_progress": "implement", "completed": "verify", "user_decision_required": "wait-user", "retryable_failure": "repair", "provider_failure": "retry-provider", "environment_failure": "blocked", "blocked": "blocked" } },
    "verify": { "type": "verifier", "transitions": { "completed": "done", "retryable_failure": "repair", "provider_failure": "retry-provider", "environment_failure": "blocked", "blocked": "blocked" } },
    "repair": { "type": "agent", "maxAttempts": 3, "exhaustedTo": "blocked", "transitions": { "in_progress": "repair", "completed": "verify", "user_decision_required": "wait-user", "retryable_failure": "repair", "provider_failure": "retry-provider", "environment_failure": "blocked", "blocked": "blocked" } },
    "retry-provider": { "type": "system", "maxAttempts": 3, "exhaustedTo": "blocked", "transitions": { "in_progress": "implement", "provider_failure": "retry-provider", "environment_failure": "blocked", "blocked": "blocked" } },
    "wait-user": { "type": "decision", "transitions": { "in_progress": "implement", "completed": "implement", "user_decision_required": "wait-user", "blocked": "blocked" } },
    "blocked": { "type": "terminal", "transitions": {} },
    "done": { "type": "terminal", "transitions": {} }
  }
}
